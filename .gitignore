__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/
*.egg

.venv/
venv/
.env
.env.local

.conversus/output/
examples/output/
*.log

.pytest_cache/
.coverage
htmlcov/
coverage.xml

.mypy_cache/
.ruff_cache/

.DS_Store
Thumbs.db

# Desktop Extension build artifacts
conversus*.mcpb
site/
uv.lock
.aider*

# Performance budget report — emitted by engine/tests/test_validator_performance.py
# and uploaded as a CI artifact, not committed.
validator-performance-report.json

# Working-session scratch files. The `.tmp-` prefix is reserved for audit
# transcripts, inventory drafts, and other artifacts that should never be
# committed. A single `.tmp-` rule prevents a careless `git add .` from
# leaking session work.
.tmp-*
