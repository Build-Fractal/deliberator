### Dangerous Contradictions

- **["Arbiter" terminology collision creates a destructive synthesis path]**
  - **spec-061-completeness-auditor claims**: The §3.1.9 P1 row for "arbiter influence=binding vs advisory | Output headings change per influence level" is uncovered at the pipeline level. "Neither verifies that the rendered `resolution.md` file actually contains different heading text across influence levels." It recommends adding a `test_phases.py TestArbitration` test using `_make_arbiter_config` with `influence='advisory'`, running the pipeline, and asserting heading differences in `arbitration/resolution.md`. (Missed Opportunities: "Arbiter influence variant pipeline output not tested"; Rec 2)
  - **script-end-to-end-tester claims**: The strip script's L68-78 substitutions targeting `"The \d{4}-\d{2}-\d{2} deliberation arbiter explicitly extended this scope to"` and `"The \d{4}-\d{2}-\d{2} deliberation arbiter ruled"` are unreachable dead code on post-v3.1.0 constitutions "because the sentences they targeted were moved from normative body text into Amendment record blocks in cycle 2B (v3.1.0), and those blocks are already consumed by the Amendment record regex earlier in Step 3b." (Missed Opportunities: "Dead code: arbiter-ruling substitutions")
  - **Why this is dangerous**: A synthesis agent reading both reviews might interpret these as related symptoms of a single "arbiter not handled correctly" defect and propose a unified fix: restore arbiter-ruling sentences to the normative body of XXIV, XXV, XXVII, making the dead code active again and simultaneously providing body text for pipeline assertions to target. This would be catastrophically wrong. It would undo the v3.1.0 Amendment record migration, break the strip script's correct stripping behavior (Amendment records exist precisely to encapsulate those sentences away from normative body text), and still not produce the correct pipeline test because `resolution.md` is a deliberation engine output file written by `engine/phases.py` based on `ArbiterConfig.influence` — it has no connection to constitutional text. The strip script's dead code concern is about regex cleanup for pre-v3.1.0 constitutional prose. The pipeline test gap is about the deliberation engine rendering influence-level headings in a run artifact. These are orthogonal problems in different file systems.
  - **Suggested resolution**: Both observations are correct and must be treated as independent action items with explicit file scope separation. (A) Strip script L68-78: add a comment documenting that these substitutions target pre-v3.1.0 body text and are no-ops on post-v3.1.0 constitutions (script-end-to-end-tester Rec 3, files: `scripts/strip-constitution-for-blind.py` only). (B) Pipeline influence test: file a follow-up issue and add `influence='advisory'` test to `test_phases.py TestArbitration` (spec-061-completeness-auditor Rec 2, files: `engine/tests/test_phases.py` only). The synthesis must explicitly name both files and forbid cross-contamination.

- **[Strip script CI smoke test conceals the leakage bug it purports to validate]**
  - **spec-061-completeness-auditor claims**: The three-tier evals.yml structure "correctly implements the 'separate, manually-triggered job' that §4.3 and Principle XXIV require for cost-bearing tests." It accepts the smoke tier's design without modification. (Alignment: "Three-tier cost partitioning matches §4.3's marker taxonomy")
  - **script-end-to-end-tester claims**: Rec 7 proposes adding a CI step to the smoke tier: `python scripts/strip-constitution-for-blind.py --input CONSTITUTION.md --output /tmp/CONSTITUTION-blind.md --version v$(...)`. Separately and at higher priority, Rec 1 identifies that passing `--date 2026-05-01` to the strip script exits code 1 because prior SIR blocks contain that date — "the blind verification workflow is blocked." (Rec 1: P1; Rec 7: P2)
  - **Why this is dangerous**: The proposed smoke test in script-end-to-end-tester Rec 7 passes only `--version`, not `--date`. A strip run without `--date` exercises `verify_zero_leakage`'s version-string path only, not its date-checking path. The CI step would exit code 0 and write the output file every push, appearing to validate the strip script's production readiness. A maintainer seeing green on this CI step — reinforced by spec-061-completeness-auditor's acceptance of the smoke tier as correctly designed — would conclude the strip script and its verification are operational. The date leakage check remains broken and undetected by CI. The false positive is especially dangerous because the date leakage bug (script-end-to-end-tester Rec 1) is the only thing blocking any blind verification run for the current v3.1.x amendment cycle.
  - **Suggested resolution**: Script-end-to-end-tester's Rec 7 must be amended before implementation. The CI step should run in two stages: (A) strip without `--date` and assert exit code 0 (validates version-string stripping); (B) strip with `--date 2026-05-01` and assert exit code 1 (documents that the date leakage check correctly fails until Rec 1 is applied). When Rec 1 is implemented, stage (B) flips to assert exit code 0. This creates a CI gate that enforces Rec 1 rather than concealing its absence.

- **[Step 14 MATCH verdict accepted without acknowledging the upstream workflow blocker]**
  - **spec-061-completeness-auditor claims**: Step 14's PARTIALLY DONE marker is "accurately self-assessed" (MATCH verdict). The persist→list→show API round-trip tests are done. The CI/CD exit code scheme is "blocked on spec 048's gate command landing." This blocking state is correctly documented in the step 14 annotation. (Summary Verdicts: Step 14 MATCH; Alignment: "PARTIALLY DONE marker accurately self-assessed")
  - **script-end-to-end-tester claims**: The strip script's `verify_zero_leakage` function exits code 1 when `--date 2026-05-01` is passed, blocking strip output file creation entirely. This means no blind verification run can complete for any v3.1.x amendment, which means no constitution ratification cycle can complete, which means the governance gate that step 14's exit codes would gate has no functional input pipeline. (Rec 1: P1 — "any operator who follows the docstring's usage example and passes `--date 2026-05-01` cannot produce a stripped output file at all")
  - **Why this is dangerous**: spec-061-completeness-auditor's MATCH verdict for step 14 treats step 14's status as complete given what the spec says about that step's own scope. But the "governance exit codes" that step 14 is building are meaningless if the strip script blocks every blind verification run that would trigger those exit codes. Implementing step 14's exit code emission (currently blocked on spec 048) without first fixing the strip script's leakage check would produce a governance gate that emits structured exit codes into a workflow that cannot complete. The MATCH verdict is technically accurate for step 14's own scope but operationally misleading about the workflow's health.
  - **Suggested resolution**: The synthesis should add a sequencing constraint that doesn't appear in either review independently: script-end-to-end-tester's Rec 1 (date leakage fix) is a prerequisite for step 14's governance exit code work, not a parallel recommendation. The step 14 annotation should be updated to note: "governance exit code emission presupposes a functional blind verification pipeline; strip script date leakage check (tracked in separate issue) must be resolved first."

---

### Tensions

- **[Priority label inflation: "P1" means operational blocker in one review, coverage gap in another]**
  - **spec-061-completeness-auditor's position**: Four of eight recommendations are P1: cost-estimate assertion (Rec 1), arbiter influence output test (Rec 2), Node version spec update (Rec 3), step 13 annotation correction (Rec 4). In each case, P1 means "a §3.1.9-marked requirement currently has zero test coverage" or "a Principle XIV parity violation." Tests exist for adjacent scenarios; the P1 scenario is simply not exercised. (Summary Verdicts section; Rec 1-4)
  - **script-end-to-end-tester's position**: Two of seven recommendations are P1: exclude HTML comments from date leakage check (Rec 1), add test suite (Rec 2). In both cases, P1 means "the tool exits with an error code or has no validation at all when used according to its own documented interface." These are operational failures, not coverage gaps.
  - **Nature of tension**: When a synthesis agent assigns implementation order by P1 priority across both reviews simultaneously, it will place spec-061-completeness-auditor's annotation documentation update (Rec 4: step 13 annotation correction) at the same priority level as script-end-to-end-tester's workflow unblocking fix (Rec 1: date leakage check). These are not equivalent. One is a documentation clarification; the other prevents the tool from writing its output file. The priority labels are internally consistent within each review but incompatible across them.
  - **Coordination needed**: The synthesis must establish a meta-priority hierarchy: operational failures (tool exits wrong, workflow blocked) are P0 relative to any review's P1 designation. script-end-to-end-tester's two P1 recommendations should be resolved before spec-061-completeness-auditor's four P1 recommendations. The sequencing rationale: a broken strip pipeline means no blind verification runs, which means no constitutional data for any of spec-061-completeness-auditor's test coverage gaps to exercise.

- **[CI expansion direction: depth (more providers) vs. scope (new artifact class)]**
  - **spec-061-completeness-auditor's position**: Rec 5 proposes a `provider-matrix` CI job running `--provider openai` and `--provider gemini` with real API keys. Rationale: "§3.1.2's `openai (OPENAI_API_KEY)` and `gemini (GOOGLE_API_KEY)` providers are never exercised in CI with real credentials." This expands coverage depth within the existing engine test domain. (Missed Opportunities: "OPENAI_API_KEY and GOOGLE_API_KEY not wired in CI"; Rec 5: P2)
  - **script-end-to-end-tester's position**: Rec 7 proposes adding a strip script step to the smoke tier. Rationale: the script has zero CI coverage and a format regression would only be detected at blind verification time. This expands CI scope to a new artifact class (governance tooling). (Rec 7: P2)
  - **Nature of tension**: Both are P2 and both require evals.yml changes, but they pull in different directions. The provider-matrix job requires secret provisioning, a new job definition, and ongoing secret rotation — high maintenance. The strip script step requires no secrets and is zero-cost — low maintenance. If there is a bounded capacity for evals.yml additions before the next release, treating them as equal P2 items creates an unexamined trade-off: adding the high-maintenance item first could saturate the capacity before the low-maintenance item lands.
  - **Coordination needed**: The synthesis should separate these decisions: (A) strip script CI step — zero-credential, smoke tier, implement immediately after script-end-to-end-tester Rec 1 is resolved; (B) provider-matrix job — requires secret provisioning decision, implement as a separate PR. Sequencing: (A) before (B). The fact that both are labeled P2 should not be read as "implement in arbitrary order."

- **[Test file location and ownership: engine/tests/ vs. scripts/tests/]**
  - **spec-061-completeness-auditor's position**: All recommended tests belong in `engine/tests/`: `test_sdk.py TestCostEstimate` (Rec 1), `test_phases.py TestArbitration` (Rec 2), and implicit `test_cli_context.py` extension (Rec 6). This is consistent with the existing test framework structure and the pytest configuration in `pyproject.toml`. (Recs 1, 2, 6)
  - **script-end-to-end-tester's position**: Rec 2 proposes creating `scripts/tests/test_strip_constitution.py` in a new `scripts/tests/` directory that does not currently exist. This is a new test directory outside `engine/tests/`. (Rec 2: "or `tests/test_strip_constitution.py` if a root-level test directory is preferred")
  - **Nature of tension**: `engine/tests/` is configured for pytest with a known `conftest.py`, `_CASCADE_ENV_VARS`, and `clean_settings` fixture. A new `scripts/tests/` directory would need its own pytest configuration, its own fixtures for `CONSTITUTION.md` path resolution, and a separate invocation path. The `engine/tests/` suite is already discovered by `uv run pytest engine/tests/`. The strip script tests may or may not be discovered by the same command — this is unspecified in script-end-to-end-tester's Rec 2.
  - **Coordination needed**: Before implementing script-end-to-end-tester Rec 2, the synthesis should decide: should the strip script tests run in the same `uv run pytest` invocation as `engine/tests/`, or separately? If separately, what is the CI step that invokes them? If together, should they live at a root-level `tests/` directory instead of `scripts/tests/`? The answer affects the smoke tier CI step proposed in Rec 7. This coordination is absent from both reviews.

- **[XIX sub-headings: undocumented omission in strip script vs. accepted structural reality in spec]**
  - **spec-061-completeness-auditor's position**: Applies Principle XIV (Spec-Implementation Parity) to identify the Node 18→22 deviation as a REVISE finding. The principle: "If the implementation deviates... update the spec to match reality." (Step 10 REVISE; Rec 3)
  - **script-end-to-end-tester's position**: The `#### Architectural invariants` and `#### Operational constants` sub-headings in Principle XIX are preserved in stripped output without documentation of that decision. Classifies this as P3: "If preservation is intentional..., document this decision in the script." (Missed Opportunities: "XIX sub-headings expose structural amendment history"; Rec 6)
  - **Nature of tension**: spec-061-completeness-auditor's consistent application of Principle XIV would, if extended to the strip script domain, classify the undocumented XIX sub-heading preservation as a REVISE item — the same classification it assigned to Node 18→22. But spec-061-completeness-auditor doesn't cover the strip script domain, so it doesn't apply this logic there. Script-end-to-end-tester classifies the same class of problem as P3 in the strip script domain. The severity asymmetry (REVISE vs. P3) is not justified by any difference in the nature of the deviation — both are "implementation does X, documentation implies Y."
  - **Coordination needed**: The synthesis should apply Principle XIV consistently across both artifacts. Node 18→22 and XIX sub-heading preservation are both undocumented implementation decisions; both should be documented at the same priority. If spec-061-completeness-auditor's REVISE for Node 18→22 is accepted as P1 (Rec 3), then script-end-to-end-tester's P3 for XIX sub-heading preservation should be upgraded to match. Alternatively, both can remain at their respective reviewers' priority assessments as long as the synthesis notes they are the same class of defect.

---

### Safe Agreements

- **[The strip script has no automated test coverage and this is the highest-confidence actionable finding]**
  - **Shared position**: Both reviews, from entirely different analytical angles, identify that the strip script's regex pipeline has no automated test coverage and that this gap should be resolved before the next blind verification run. Script-end-to-end-tester states directly: "The script has zero automated test coverage. Every regex in the strip pipeline is exercised only by ad-hoc manual runs. Given the multi-version format hazards introduced in v3.1.x..., the absence of tests means regressions in any future strip-script update would go undetected until a blind verification run fails." (Executive Summary; Rec 2: P1). Spec-061-completeness-auditor does not examine the strip script, but its entire analytical methodology — applied to spec 061's engine tests — demonstrates that "only unit-tested it on a sample" (the PR #103 claim that justified the Amendment record regex) is precisely the kind of coverage claim it would flag as insufficient. Spec-061-completeness-auditor's Missed Opportunity on "iterations=3 cost-estimate assertion absent" follows the same logic: a regex or formula that is "unit-tested on a sample" but has no automated test covering the specific scenario is a gap, regardless of what the PR description claims.
  - **Combined evidence**: Script-end-to-end-tester provides empirical evidence: specific v3.1.x format hazards (Amendment records, prior SIR date leakage, XXV Pattern 2 coverage, XIX sub-headings) that are covered only by manual trace, not automated assertion. Spec-061-completeness-auditor provides methodological precedent: its entire review demonstrates that the project's standard for "covered" requires an automated test that would fail if a regression were introduced, not a description in a PR body. Together they establish: the strip script meets neither the empirical standard (known format hazards have no automated assertions) nor the methodological standard (PR #103's "unit-tested it on a sample" is the same evidence quality spec-061-completeness-auditor rejected for the cost-estimate assertion).
  - **Confidence level**: High. This agreement is reinforced by the fact that both reviews reach it through independent reasoning with no shared source. The synthesis should treat this as the finding most likely to survive revision: even if specific recommendations are modified, "add automated tests for the strip script before the next blind verification run" should remain.

- **[Spec-implementation parity violations are a recurring structural pattern, not isolated incidents]**
  - **Shared position**: Both reviews independently identify instances where an authoritative document says X and the implementation does Y without documentation of the deviation. Spec-061-completeness-auditor identifies this in evals.yml vs. spec 061 step 10 (Node 18→22), citing Principle XIV by name: "A spec that says X when the implementation does Y is a bug in the spec." (Step 10 REVISE; Rec 3). Script-end-to-end-tester identifies this in the strip script's docstring vs. its actual behavior: the prior SIR preservation is intentional but undocumented; the dead code in L68-78 is harmless but undocumented; the usage example in the docstring implies a simpler constitution than the v3.1.x format actually requires. (Recs 3, 4, 6)
  - **Combined evidence**: Spec-061-completeness-auditor's citation of Principle XIV gives the shared problem a constitutional name and establishes it as a P1-class violation. Script-end-to-end-tester's three examples of the same pattern in the strip script's docstring demonstrate that this is not an isolated CI misconfiguration but a project-wide tendency: implementation diverges from its documented contract, and neither the spec nor the documentation acknowledges the divergence. The combined finding is stronger than either alone: it shows that Principle XIV violations occur in both spec-facing artifacts (evals.yml) and implementation-facing artifacts (the strip script).
  - **Confidence level**: High. The two reviews identify the same class of defect independently across different artifacts and one review provides the constitutional authority (Principle XIV) that governs both. The synthesis should apply Principle XIV as a blanket recommendation: audit all project artifacts where "implementation does X, documentation implies Y" and resolve the discrepancies.

- **[The three-tier CI structure is sound and the strip script step belongs in the smoke tier]**
  - **Shared position**: Spec-061-completeness-auditor accepts the evals.yml smoke/quality/deepeval structure as correctly implementing §4.3's marker taxonomy, noting that "workflow_dispatch gating for Ollama and deepeval tiers correctly implements the 'separate, manually-triggered job' that §4.3 and Principle XXIV require for cost-bearing tests." (Alignment: "Three-tier cost partitioning"). Script-end-to-end-tester proposes adding the strip script step to the smoke tier specifically, with the implicit rationale that strip operations are zero-cost, zero-credential, and deterministic — criteria that qualify a step for the smoke tier under §4.3's taxonomy. (Rec 7: P2, proposing smoke tier placement)
  - **Combined evidence**: Spec-061-completeness-auditor validates the tier design principles that determine which steps belong where. Script-end-to-end-tester validates that the strip script operation satisfies all smoke-tier criteria (no API credits, no subprocess spawning, no secret provisioning, deterministic output). Together they establish that adding the strip script to the smoke tier is architecturally correct under §4.3's taxonomy — not merely expedient but conformant with the tier structure's documented intent. Neither review identifies any reason the strip script step should go in the quality or deepeval tier.
  - **Confidence level**: Medium. The agreement on placement is high-confidence. The agreement on the specific implementation of that step is not established — as Contradiction 2 above identifies, the proposed implementation in script-end-to-end-tester Rec 7 conceals the date leakage bug. The synthesis should accept the agreement on tier placement while requiring that the step's implementation be amended per Contradiction 2's resolution before the step is added to CI.