### Dangerous Contradictions

- **Step 14 Bundling: Legitimate Partial Delivery vs. False-Completeness Vector**
  - **spec-061-completeness-auditor claims**: "PARTIALLY DONE marker accurate; API-level done, CLI surface and combinatorial matrix correctly identified as open" — Step 14 Summary Verdict: MATCH. The auditor accepts the orchestrator's sub-item decomposition as a valid split and recommends only documentation of the bundle intent (Recommendation 8: "Document the step 14 bundle intent to prevent sub-item-closing the full step").
  - **sub-step-skeptic claims**: "Step 14's bundling was intentional — these three things were meant to land together as the 'smoke combinations' tier — and the orchestrator's split creates a false-completeness vector for sub-step 14a." The step's purpose was the CI/CD consumer contract as a coherent unit, not three independently closeable deliverables. (Off-Base Assumptions §3)
  - **Why this is dangerous**: If the auditor's MATCH verdict is adopted, then the persistence sub-item is legitimately marked done and removed from the open-work queue. The auditor's Recommendation 8 softens this by adding bundle-intent documentation, but if that recommendation is not implemented atomically with the MATCH verdict, a release gate reviewer will see "Step 14: PARTIALLY DONE (persistence: done)" and treat the step as in-flight with two remaining sub-items. The skeptic's framing makes both remaining sub-items structurally absent — no exit codes, no matrix — and identifies that the PARTIALLY DONE acceptance implicitly authorizes indefinite deferral. The specific failure mode: step 14's combinatorial matrix (the only deliverable that provides cross-axis smoke coverage) has no tracking issue, no plan, and is now separated from the sub-item that shipped. Future contributors close the remaining sub-items individually; the matrix is never implemented.
  - **Suggested resolution**: The auditor's Recommendation 8 is the right mechanism but needs the skeptic's framing injected into it: the spec annotation must explicitly state that persistence-sub-item delivery does NOT close step 14, and that the exit-code and matrix sub-items require independent tracking issues before step 14 can be marked done. The skeptic yields on whether PARTIALLY DONE was appropriate as a snapshot; the auditor yields on whether that snapshot creates a long-term false-closure risk without explicit tracking infrastructure.

- **Step 10 Closure Legitimacy: Substantive DONE vs. Inflated Attribution**
  - **spec-061-completeness-auditor claims**: Step 10 verdict is REVISE — the implementation is substantively complete but the Node 18→22 discrepancy is "a Principle XIV violation" requiring a spec annotation correction. The step is treated as closed for implementation purposes pending the annotation fix. (Summary Verdicts, Step 10: REVISE; Missed Opportunities §7)
  - **sub-step-skeptic claims**: "PR #83 established the smoke tier; PR #101 added the quality (Ollama) and deepeval tiers... The step 10 description credits PR #101 for the 'three-tier' workflow, including the smoke tier PR #83 built. This inflates the orchestrator's contribution." Step 10 was already substantially done before the orchestrator touched it; PR #101's actual contribution is the quality and deepeval tiers. (Missed Opportunities §5; Actionable Recommendations §6)
  - **Why this is dangerous**: These produce different correction protocols. The auditor's REVISE verdict means: fix the Node version annotation and step 10 is closed. The skeptic's attribution concern means: even after fixing Node, the DONE annotation credits the wrong PR for the wrong work. If only the Node fix is applied, a future CI debugger who traces the smoke tier to PR #101's DONE annotation will bisect to the wrong commit when the smoke configuration breaks. More structurally: if the smoke tier pre-existed, then step 10's implementation work was smaller than the DONE annotation implies, and the velocity-driven closure the skeptic flags (Actionable Recommendations §6) applies here specifically.
  - **Suggested resolution**: Both corrections are non-exclusive. The auditor's Recommendation 7 (correct Node version) and Recommendation 6 (accurate CI contribution attribution) should be combined into a single spec annotation update. The skeptic's concern is fully resolved if Recommendation 6 is implemented: "PR #83 established the smoke tier; PR #101 added the quality and deepeval tiers and workflow_dispatch inputs." The reviews are only in tension because the auditor's summary verdicts focus on implementation correctness rather than credit attribution; both agree a correction is needed, and the combined fix satisfies both.

- **Step 9 Test Depth: Substantively Validated vs. Velocity-Suspect**
  - **spec-061-completeness-auditor claims**: Step 9 MATCH — "The implementation correctly partitions these concerns," type coercion is "fully satisfied by the existing type vocabulary," and the lockstep test "is the correct structural guard." The tests are assessed as substantively non-tautological through direct per-assertion analysis. (Alignment, Step 9)
  - **sub-step-skeptic claims**: "The cadence suggests shipping-velocity prioritization over substantive delivery... was the work substantive or were the test assertions thin?" The skeptic's spot-check standard is: "would removing one 'real-bug' line in the implementation cause at least one test to fail? If not, the test is a tautology." (Missed Opportunities §6; Executive Summary)
  - **Why this is dangerous**: Both positions cannot simultaneously govern the step 9 verdict. The auditor's MATCH verdict says the assertions are non-tautological and the step needs no further review. The skeptic's velocity concern says the step requires independent spot-checking before the MATCH verdict stands. If a release gate adopts the auditor's MATCH without the skeptic's spot-check criterion, thin assertions in a future step (13, 14a) will not be caught using the same standard — because the auditor's analysis provides per-step coverage rather than a repeatable lint standard. The specific failure mode: the skeptic's spot-check principle, if not formalized, remains an ad-hoc tool that doesn't scale to steps 10, 13, and 14a.
  - **Suggested resolution**: The skeptic's velocity concern for step 9 specifically is resolved by the auditor's detailed per-assertion analysis. The resolution is to adopt the auditor's analysis as the canonical validation that the skeptic's concern is addressed for step 9, while adopting the skeptic's spot-check principle as the verification standard for all remaining steps. The skeptic yields on step 9; the auditor yields on the general principle. Neither position is abandoned.

---

### Tensions

- **G2/G12 Severity: Missed Opportunity vs. Architectural Blocker**
  - **spec-061-completeness-auditor's position**: G2 and G12 being open while later steps are closed creates risk — "any quality baseline captured before step 6 closes will reflect a broken provider matrix" and "a CI that reports 'smoke tests pass' while G12 is open is a false health signal." Auditor recommends blocking steps 7/8 on step 6 completion (Recommendation 4) but does not revise the DONE markers already given for steps 9, 10, 13. (Missed Opportunities §3; Recommendation 4)
  - **sub-step-skeptic's position**: G12/G2 openness is an architectural blocker: "the engine ships to the smoke CI tier with a known parse-time defect that silently rejects claude-code, gemini, and every non-anthropic/openai provider in any YAML-configured run — while the step count displays eight strikethroughs." This is framed as the §7 header being actively misleading, not merely incomplete. (Executive Summary; Actionable Recommendations §1)
  - **Nature of tension**: Both reviews agree G12 is dangerous and that closing steps 9/10/13 before step 6 creates a false health signal. The tension is in consequences: the auditor treats existing DONE markers as accurate snapshots that need prospective blocking annotations; the skeptic treats the aggregate effect of the DONE markers as a misleading narrative that prospective annotations may not correct. These pull in different directions: the auditor's framing says "the markers are fine; add warnings." The skeptic's framing says "the warnings may not be seen because the markers already convey completeness."
  - **Coordination needed**: Both reviews recommend blocking steps 7/8 on step 6. The additional coordination needed is agreement on whether existing DONE markers require revision in light of G12's open status, or whether a §7 header update plus prerequisite annotations is sufficient. The auditor's approach is more actionable (revising merged DONE markers creates git history confusion). The skeptic's concern is better served by requiring prominent, prominent annotation — not a footnote — that G12/G2 remaining open while later steps are closed represents a known architectural risk. The header rewrite the skeptic recommends in Actionable Recommendations §1 would satisfy both if its language is strong enough.

- **Step 11 Deferral: Track-and-Wait vs. SC#3 Unreachability**
  - **spec-061-completeness-auditor's position**: "File a tracking issue for step 11 and mark step 12 blocked" — once the issue is filed, the gap is managed. "An untracked deferral of an architectural prerequisite is functionally abandoned." The auditor's Recommendation 3 frames the tracking issue as the resolution. (Missed Opportunities §3; Recommendation 3)
  - **sub-step-skeptic's position**: "Step 12 requires step 11 because the CLI-vs-skill parity test requires a skill surface to compare against. Without step 11, step 12 has no implementation path. Success criterion #3 ('CLI, MCP, and SDK surfaces produce structurally identical output') is therefore unreachable." The issue is not just tracking — it's that SC#3 cannot close without architectural work that has no plan. (Missed Opportunities §3; Actionable Recommendations §3)
  - **Nature of tension**: Both agree step 11 needs a tracking issue. The tension is in consequence. The auditor says filing the issue resolves the abandonment risk. The skeptic says filing the issue is necessary but not sufficient — SC#3's unreachability means spec 061 cannot be closed as complete even with all remaining steps done. These have different urgency implications: the auditor's framing allows step 11 to be treated as a planned-but-deferred item. The skeptic's framing says the deferral structurally blocks spec closure.
  - **Coordination needed**: The auditor's Recommendation 3 should be strengthened with the skeptic's SC#3 consequence. The tracking issue filed for step 11 must explicitly note that SC#3 cannot be marked ACHIEVED until step 11 lands. Without this, the issue can be deferred indefinitely without triggering a spec-closure gate failure. The auditor handles the mechanics; the skeptic handles the stakes. Both are needed.

- **Smoke Marker: CI Fix Now vs. Taxonomy Fix First**
  - **spec-061-completeness-auditor's position**: Fix the immediate problem: change the pytest command to add `-m "not integration and not live and not eval"`. This corrects the marker taxonomy violation for existing tests. (Recommendation 2; Missed Opportunities §2)
  - **sub-step-skeptic's position**: The taxonomy itself is the gap: §4.3 defines four markers but step 14's "6 cross-axis smoke combinations" implies a `smoke` marker that doesn't exist. The CI fix addresses only the symptom; the taxonomy omission creates an open question for future step-14 implementors. (Missed Opportunities §4; Recommendation 5)
  - **Nature of tension**: Both note the `@pytest.mark.integration` tests running in the smoke tier. The tension is in which fix comes first. The CI command fix (auditor) addresses the immediate taxonomy violation. The marker taxonomy extension (skeptic) addresses the future implementation ambiguity. These are not mutually exclusive but have different preconditions: the CI fix can land immediately; the taxonomy fix requires a constitutional amendment (per Principle XXV: "Adding a new top-level marker requires a constitutional amendment — the taxonomy is the contract"). The skeptic's fix has a higher barrier than the auditor's.
  - **Coordination needed**: Implement the auditor's CI fix first (immediate, low-barrier, fixes the current violation). Then decide whether to add `smoke` to §4.3 via constitutional amendment or rename step 14's deliverable to use an existing marker (the auditor's alternative in Recommendation 5). The decision must precede step 14's matrix test implementation. Both reviews agree on the sequence; the coordination is ensuring the taxonomy decision is made before the matrix tests are written, not after.

- **Step 13 Coverage Framing: Two Equal P1 Gaps vs. Structural Delivery with Annotations**
  - **spec-061-completeness-auditor's position**: Two §3.1.9 P1 rows are unclosed and equally urgent: "iterations=3 cost estimate reflects 3×" (Recommendation 1) and "arbiter influence=binding vs advisory output headings" (Recommendation 2). Both are presented as requiring separate tracking issues before step 13 can be treated as complete. (Missed Opportunities §1, §2)
  - **sub-step-skeptic's position**: Step 13's structural delivery — dispatch counts, revision naming, event ordering — is real and non-tautological. The step 13 DONE annotation "conflates trigger-condition coverage with influence-row coverage." The two unclosed rows are framing omissions in the annotation, not evidence that the structural delivery is incomplete. (Off-Base Assumptions §1)
  - **Nature of tension**: Both agree the two P1 gaps exist. The tension is in what they mean for step 13's status. The auditor treats both gaps as blocking the REVISE verdict — i.e., step 13 is REVISE until both issues are filed. The skeptic treats the structural delivery as real, with the gaps as annotation corrections. These produce different verdicts: REVISE (auditor) vs. effectively "DONE with known open rows" (skeptic). These pull in different urgency directions for follow-up work.
  - **Coordination needed**: Agree on the step 13 annotation language. The most accurate formulation: "step 13 delivers the structural iteration contract (dispatch counts, naming, events); two §3.1.9 P1 rows (cost estimate and influence output headings) remain open and are tracked in issues #N and #M." This acknowledges the real delivery (satisfying the skeptic's framing) without implying the unclosed rows are minor (satisfying the auditor's REVISE verdict). The annotation update and the two tracking issues should land as a single atomic change.

---

### Safe Agreements

- **Node Version Spec-Implementation Parity Requires Correction**
  - **Shared position**: Both reviews independently identify the Node 18 (spec) vs. Node 22 (implementation) discrepancy as a Principle XIV violation. The auditor: "Node version drift between spec and implementation" requires updating step 10's annotation to reflect Node 22 (Missed Opportunities §7; Recommendation 7). The skeptic: "Spec §7 step 10 states 'specify runner requirements: Python 3.12, Node 18.' `.github/workflows/evals.yml` uses `node-version: '22'` throughout with no inline comment... Principle XIV: 'If the implementation deviates, update the spec to match reality.'" (Missed Opportunities §6; Recommendation 7)
  - **Combined evidence**: The auditor establishes the spec citation text (step 10: "Node 18") and the implementation fact (evals.yml: `node-version: '22'`). The skeptic establishes the constitutional grounding and the risk ("A future CI maintainer sees 'Node 18' in the spec, pins `node-version: '18'` restoring EOL behavior, and breaks the pipeline silently"). Together, the factual discrepancy, the governance obligation, and the concrete failure scenario are all covered without either review needing to be comprehensive on its own.
  - **Confidence level**: High. Both reviews reach the same conclusion through independent paths and cite the same evidence. The fix is unambiguous, low-risk, and has no architectural preconditions.

- **Smoke Marker Taxonomy Gap Requires Resolution Before Step 14 Matrix Implementation**
  - **Shared position**: Both reviews identify the absence of `@pytest.mark.smoke` in §4.3's taxonomy as a gap that will cause implementation ambiguity when step 14's cross-axis matrix tests are written. The auditor: "6 cross-axis smoke combinations has no matching marker... When the combinatorial matrix is implemented, the developer will have no sanctioned marker to attach." (Missed Opportunities §6; Recommendation 5). The skeptic: "The absence of a smoke marker is itself a gap" and notes that marking them `integration` (pre-release) conflicts with the every-push intent of smoke-tier tests. (Missed Opportunities §4; Recommendation 5)
  - **Combined evidence**: The auditor traces the gap to §4.3's four-marker taxonomy and the CI job labeled "Smoke tests" using no marker annotation. The skeptic traces the gap to step 14's "6 cross-axis smoke combinations" language implying a marker that doesn't exist in §4.3. Both independently document that without a sanctioned marker, future contributors will attach the wrong marker (creating a per-release-only cadence for tests intended to run on every push) or leave tests unmarked (running them in all tiers including live). The combined evidence covers both the taxonomy source and the downstream misclassification consequences.
  - **Confidence level**: High. Both reviews agree the gap is real and that a resolution must precede step 14's matrix implementation. Neither review is internally inconsistent on this point.

- **Integration Tests in Smoke Tier Violate the Marker Contract**
  - **Shared position**: Both reviews confirm that `@pytest.mark.integration` tests are running in the smoke CI tier, violating the §4.3 taxonomy contract. The auditor names the specific violation: "`TestPersistListShowRoundTrip` is `@pytest.mark.integration` and currently runs on every push" and recommends a CI command fix (Recommendation 2; Missed Opportunities §2). The skeptic frames the same problem as the taxonomy taxonomy being "violated by the CI that was built to enforce it" (Actionable Recommendations §2).
  - **Combined evidence**: The auditor provides the specific CI command (`uv run pytest engine/tests/ -x -q --timeout=30` with no `-m` filter) and the specific test class that violates the contract. The skeptic's analysis of PR #106's tests in `test_persistence.py` provides independent confirmation that `@pytest.mark.integration` is present on `TestPersistListShowRoundTrip`. Together, both reviews confirm the factual state, name the responsible test class, and agree on the fix (a single CI command-line change).
  - **Confidence level**: High. Both reviews independently verify the same factual state. The fix requires no architectural decisions and can land immediately without preconditions.

- **Step 11/12 Dependency Requires Tracking Infrastructure**
  - **Shared position**: Both reviews agree that step 11's deferral without a tracking issue leaves step 12 (and SC#3) without an implementation path, and that filing a tracking issue is the minimum corrective action. The auditor: "File a tracking issue for step 11 and mark step 12 blocked" — "An untracked deferral of an architectural prerequisite is functionally abandoned." (Recommendation 3; Missed Opportunities §3). The skeptic: "File a tracking issue for step 11 with scope boundaries. Add to step 12 in the spec: 'BLOCKED on step 11 (GitHub issue #N).' Add to §6 criterion 3: 'Not achievable until step 11 lands.'" (Actionable Recommendations §3)
  - **Combined evidence**: The auditor establishes the architectural dependency chain (step 12 requires step 11; SC#3 requires step 12). The skeptic establishes the process gap (no tracking vehicle = functionally abandoned) and adds the SC#3 consequence (spec 061 cannot close as complete without step 11). Together, both the technical dependency and the process risk are documented. The skeptic's SC#3 language strengthens the auditor's tracking-issue recommendation by providing a spec-closure gate that prevents indefinite deferral.
  - **Confidence level**: High. Both reviews reach the same recommendation with complementary evidence. The variation in scope (auditor: tracking issue + step 12 blocked; skeptic: tracking issue + step 12 blocked + SC#3 blocked) is additive rather than conflicting.