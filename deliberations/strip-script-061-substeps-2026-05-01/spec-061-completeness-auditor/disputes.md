### Remaining Disputes

- **Dispute: Step 10 Annotation Correction — Standalone PR vs. Bundled Delivery**
  - **My claim**: The two Step 10 annotation corrections (Node 18→22 and PR attribution: smoke tier = PR #83, quality/deepeval = PR #101) must land atomically with each other in a standalone PR and should not wait for future CI execution changes. (My revision, Rec 3: "Both corrections must land atomically. The Principle XIV rationale from my original review applies to both.")
  - **Opposing position(s)**: Sub-step-skeptic Rec 6 (modified) states: "the PR that implements any further changes to evals.yml's smoke job (including strip-script bash step if pursued) should apply the step 10 attribution correction atomically, naming all contributing PRs." This defers the attribution correction until a future PR that also adds the strip-script bash step — which itself requires sub-step-skeptic's marker filter fix (Rec 2) to land first.
  - **Why I will not concede**: The annotation corrections are pure prose edits to `specs/061-engine-eval-suite.md` — no code changes, no evals.yml modifications, no dependency on any CI execution change. Sub-step-skeptic's bundling logic creates a delivery chain: annotation fix waits on marker filter (Rec 2) waits on strip-script bash step coordination. While that chain is internally coherent for CI mechanics, it delays a Principle XIV correction (a spec that says "Node 18" when the implementation uses Node 22 is a bug in the spec) for an indefinite period. The longer the incorrect attribution survives, the higher the probability that a future CI maintainer pins `node-version: '18'` — restoring EOL behavior — based on the spec's authority. Annotation bugs compound; CI coordination windows are bounded.
  - **Counter-argument to their position**: Sub-step-skeptic frames bundling as avoiding "two sequential single-PR DONE note corrections." But the alternative is not "two sequential corrections" — it is "one standalone annotation PR, then one CI mechanics PR." The coordination cost of a two-PR sequence is low; the risk of indefinite deferral created by making the annotation fix dependent on CI PRs that involve multiple reviewers and sequencing constraints is not low. Sub-step-skeptic's own revision identifies the Node 18 EOL date as "already-past" — this is not a theoretical future risk. Waiting for bash-step coordination to fix a past-EOL spec claim is the wrong tradeoff.
  - **Proposed resolution path**: The standalone annotation PR should land first, correcting both Node 18→22 and PR attribution. The strip-script bash step PR (when it lands, coordinating with sub-step-skeptic's marker filter) then appends its own PR to the attribution record without retroactively modifying the annotation already in place. The synthesizer must choose which sequencing model governs: annotation-first (my position) or wait-for-CI-coordination (sub-step-skeptic's position).

---

- **Dispute: §3.1.9 Gap Remediation — Tracking Issues Only vs. Issues + Immediate Test Implementations**
  - **My claim**: My Recs 1 and 2 (surviving from revision) call for filing GitHub issues AND simultaneously adding concrete test implementations — `test_sdk.py` gets a parametrized `TestCostEstimate` variant asserting `cost_estimate["cross_review"] == 6` for `iterations=3` (Rec 1), and `test_phases.py TestArbitration` gets an `influence="advisory"` test asserting output heading differences (Rec 2). Both use the mock provider and bypass G12 entirely.
  - **Opposing position(s)**: Sub-step-skeptic New Rec A says: "File two GitHub issues against step 13's DONE marker: (1) iterations=3 cost estimate assertion. (2) Arbiter influence=binding vs. advisory output heading." The recommendation stops at issue filing and annotation revision to DONE WITH OPEN GAPS, treating test implementation as follow-up work for the issue's assigned milestone.
  - **Why I will not concede**: Both Recs 1 and 2 are mock-provider Python-surface unit tests. The G12 and step 5 blocking rationale sub-step-skeptic applied to quality-tier work explicitly does not apply here — both sub-step-skeptic (Rec 4, modified) and I converged that "mock-provider Python-surface unit tests that invoke `run_pipeline`, `run_decide_mcp`, or handler functions directly are not blocked by G12." The implementation cost for these two tests is low; both fixture paths are already established in `test_sdk.py` and `test_phases.py`; the mock provider is already wired. Filing an issue and then closing it with a later PR introduces a delivery gap where step 13's annotation reads DONE WITH OPEN GAPS indefinitely. In safety-critical infrastructure (spec 067 / CONSTITUTION.md), DONE WITH OPEN GAPS is a weaker signal than DONE — but it is also weaker than an actually-fixed gap. The issue-only path optimizes for process hygiene at the cost of the substantive fix that the process exists to produce.
  - **Counter-argument to their position**: Sub-step-skeptic's "file issues first" recommendation is defensible for gaps where the fix is uncertain or requires design work. For Recs 1 and 2, there is no design uncertainty: the test inputs are parametrize lists that already exist in the test files, the fixture is already set up, and the assertion semantics are fully specified. Issues-only for fully-specifiable, low-complexity tests imposes a process tax without meaningful risk reduction. The analogous case in the revision history is that sub-step-skeptic itself notes the §3.1.9 arbiter influence gap "supports my original thesis that the step 13 DONE marker should be scrutinized" — that scrutiny was supplied by my Recs 1 and 2, which include the test code. Reducing them to issues strips the substantive content that made the scrutiny meaningful.
  - **Proposed resolution path**: The tracking issues should be filed AND the test implementations should be committed in the same PR. The annotation should be updated to DONE (not DONE WITH OPEN GAPS) once both land. The synthesizer must adjudicate between "file issues and treat implementation as follow-up" (sub-step-skeptic) and "file issues and implement in the same PR" (my position).

---

### Convergence

- **Converged: Step 14 Verdict — REVISE Required**
  - **Shared position**: Step 14's Summary Verdict must be changed from MATCH to REVISE. The PARTIALLY DONE marker is internally self-consistent, but a MATCH verdict removes the spec's obligation to require tracking infrastructure for the remaining sub-items (CI/CD exit codes blocked on spec 048, combinatorial matrix not yet implemented). The REVISE-class correction requires that: (a) the step 14 annotation state that persistence sub-item delivery does NOT close step 14 as a whole; (b) the exit-code and matrix sub-items each have independent GitHub issues filed with explicit blocking language; (c) the combinatorial matrix must have a tracking issue before step 14 can be indefinitely deferred without losing accountability.
  - **Agreeing agents**: spec-061-completeness-auditor (New Recommendation from revision: "Upgrade Step 14 verdict from MATCH to REVISE"); sub-step-skeptic (Rec 8 modified: "the spec annotation must state both (a)… AND (b) that the exit-code and matrix sub-items require independent GitHub issues before step 14 can be marked done").
  - **Strength**: Bilateral (two agents).
  - **Path to convergence**: My original MATCH verdict was the error; sub-step-skeptic identified that PARTIALLY DONE without tracking infrastructure creates a false-closure vector. I conceded in my revision and upgraded the verdict to REVISE. Sub-step-skeptic's modified Rec 8 strengthens the requirement beyond my original new recommendation by specifying that documentation alone is insufficient — tracking issues constitute the gate.

---

- **Converged: Step 11 / Step 12 Dependency Requires Tracking Infrastructure with SC#3 Consequence**
  - **Shared position**: File a GitHub issue for step 11 (engine-first skill wrapping CLI) that explicitly notes: step 12 (cross-surface parity tests) is blocked on step 11; Success Criterion 3 ("CLI, MCP, and SDK surfaces produce structurally identical output") is unreachable until step 11 lands; the spec §6 criterion 3 annotation must be updated to read "Not achievable until step 11 lands (GitHub issue #N)." An untracked deferral of an architectural prerequisite is functionally abandoned.
  - **Agreeing agents**: spec-061-completeness-auditor (New Recommendation from revision: "File Step 11/12 tracking issue with SC#3 consequence"); sub-step-skeptic (Rec 3, surviving: "File a GitHub issue for step 11 with scope boundaries; add BLOCKED notation to step 12 citing the issue number; add to §6 criterion 3 'Not achievable until step 11 lands'"). Sub-step-skeptic adopts my SC#3 consequence language as an addendum.
  - **Strength**: Bilateral (two agents).
  - **Path to convergence**: My new recommendation and sub-step-skeptic's Rec 3 arrived at the same conclusion via different paths (I came from the SC#3 gate analysis; sub-step-skeptic came from the "untracked deferral = functionally abandoned" principle). The two analyses are complementary; the converged position requires both the tracking issue content and the SC#3 annotation update.

---

- **Converged: G12 Blocking Scope — Narrowed to Real-Provider and YAML-Configured Paths**
  - **Shared position**: G12 (VALID_PROVIDERS hardcoded in config.py) blocks quality-tier calibration runs (step 7) and baseline snapshots (step 8) that use YAML-configured or real-provider paths. G12 does NOT block mock-provider Python-surface unit tests that invoke `run_pipeline`, `run_decide_mcp`, or handler functions directly. My Recs 1 and 2 (§3.1.9 gap-filling tests using mock provider) may proceed in parallel with G12 remediation.
  - **Agreeing agents**: spec-061-completeness-auditor (my revision: "the mock-provider path bypasses the VALID_PROVIDERS hardcoding entirely. This recommendation is unaffected by G12's open status"); sub-step-skeptic (Rec 4, modified: "the G12 prerequisite applies to (a) quality-tier calibration runs using real providers (step 7), (b) baseline snapshots (step 8), (c) any test that exercises the YAML-configured `conversus run` path… Mock-provider behavioral unit tests… are not blocked by G12").
  - **Strength**: Bilateral (two agents).
  - **Path to convergence**: Sub-step-skeptic's original Rec 4 framing was broad enough to read as blocking all new test work before step 6 closes. My analysis of the mock-provider test paths corrected that reading; sub-step-skeptic conceded and narrowed the scope in revision. The result is a more precise blocking claim that lets independent work proceed.

---

- **Converged: Node 18→22 Fix Urgency — P1 Classification**
  - **Shared position**: The Step 10 node version annotation should be corrected to "Node 22 LTS (spec said Node 18 — Node 18 EOL April 2025)" at P1 priority. Node 18 reached EOL in April 2025; any contributor following the spec today who pins `node-version: '18'` would introduce a security exposure and runtime breakage. Principle XIV governs: a spec that says X when the implementation does Y is a bug in the spec.
  - **Agreeing agents**: spec-061-completeness-auditor (my Rec 3: "Both corrections must land atomically. The Principle XIV rationale from my original review applies to both"); sub-step-skeptic (Rec 7, modified to P1: "I yield. The risk is real and the fix is trivial. Node 18 EOL is not a theoretical future risk but an already-past date.").
  - **Strength**: Bilateral (two agents).
  - **Path to convergence**: My Rec 3 applied Principle XIV and sub-step-skeptic conceded the P1 classification explicitly, citing my constitutional grounding. The technical facts (EOL date, failure mode) are not in dispute; only the delivery sequencing question remains (see Remaining Disputes above).

---

- **Converged: §3.1.9 Coverage Gaps Exist and Are P1**
  - **Shared position**: Two P1 coverage rows in §3.1.9 are genuinely unclosed at the levels the spec requires: (1) iterations=3 cost estimate assertion — no test verifies that `cost_estimate["cross_review"] == 6` for `iterations=3`; a user may see a cost estimate calibrated for 1 iteration and then pay 3×; (2) arbiter influence=binding vs. advisory output heading — all three arbitration trigger tests in `test_phases.py TestArbitration` exercise only the default binding influence level; a regression in advisory or informational heading text would pass all three tests. Step 13's annotation is therefore overclaimed and should be marked DONE WITH OPEN GAPS until these are resolved.
  - **Agreeing agents**: spec-061-completeness-auditor (Recs 1 and 2, surviving); sub-step-skeptic (New Rec A: "file two GitHub issues against step 13's DONE marker… step 13's DONE annotation should be revised to DONE WITH OPEN GAPS until these issues are filed"). The disagreement between us is about whether test implementations should be simultaneous with issue filing (see Remaining Disputes), not about whether the gaps exist or are P1.
  - **Strength**: Bilateral (two agents).
  - **Path to convergence**: Sub-step-skeptic's independent audit of the §3.1.9 matrix confirmed my original analysis row-by-row. The convergence emerged through cross-review: sub-step-skeptic reviewed my work, confirmed the two gaps, and developed complementary evidence (template-context vs. pipeline-output distinction for the influence gap). The factual basis is now twice-verified through independent analytical paths.

---

### Final Position Statement

**Non-Negotiables**

1. **Step 14 must be changed from MATCH to REVISE with explicit tracking issues for both remaining sub-items.** A MATCH verdict on a PARTIALLY DONE annotation removes the spec's obligation to require tracking infrastructure. The combinatorial matrix (6 cross-axis smoke combinations) is the only deliverable that provides cross-axis coverage and is not tracked by any issue. Without a tracking issue, a contributor who closes the exit-code sub-item when spec 048 lands has no mechanism preventing them from treating step 14 as fully done while the matrix is permanently deferred. This is not a documentation preference — it is the difference between an accountable completion path and indefinite abandonment. (My revision, New Rec 1; sub-step-skeptic Rec 8, modified.)

2. **The two §3.1.9 gaps (iterations=3 cost estimate, arbiter influence=binding vs. advisory output heading) are P1 and must be tracked with GitHub issues and the step 13 annotation must be updated to DONE WITH OPEN GAPS.** These are not speculative gaps — they are verified absences in the mock-provider test path, both confirmed independently by two agents. Step 13's DONE marker is overclaimed. The annotation correction and the tracking issues are the minimum acceptable outcome; I also hold that the test implementations should be simultaneous (see Remaining Disputes), but the annotation correction and issue filing are non-negotiable in any case. (My Recs 1 and 2, surviving; sub-step-skeptic New Rec A.)

3. **Step 11/12 tracking issue with SC#3 consequence annotation must be filed before step 14 implementation.** SC#3 ("CLI, MCP, and SDK surfaces produce structurally identical output") is unreachable without step 11. A spec that can close all remaining steps without achieving SC#3 has a structural incompleteness that is invisible in the spec itself. Filing the issue and updating the §6 annotation converts an implicit assumption into a visible gate. This costs no code changes — only two spec annotation updates and one GitHub issue. (My revision, New Rec 2; sub-step-skeptic Rec 3, surviving.)

---

**Flexibility**

1. **Step 10 annotation correction timing.** I hold that the Node 18→22 and PR attribution corrections should land in a standalone atomic PR now. I am flexible on whether that PR is filed as a separate issue (with its own tracking vehicle) or as a branch directly against `main`. What must be preserved: (a) both corrections land together in one commit, (b) they do not wait for bash-step CI additions that carry their own sequencing dependencies, and (c) the correction is filed before any contributor merges a PR against the spec that takes the Node 18 or PR #101 attribution as authoritative.

2. **§3.1.9 gap remediation delivery model.** I hold that tracking issues and test implementations should land in the same PR. I am flexible on whether this is one PR or two PRs (issues-only first, implementations second), provided the following is preserved: (a) the test implementations are committed within the same sprint as the issues (not deferred indefinitely), (b) the step 13 annotation is not advanced from DONE WITH OPEN GAPS back to DONE until both implementations are merged, and (c) the mock-provider test paths I specified (parametrized `TestCostEstimate` in `test_sdk.py`; `influence="advisory"` test in `test_phases.py TestArbitration`) are the implementations that close the issues when merged. What I cannot accept is issues filed with no committed implementation timeline, leaving DONE WITH OPEN GAPS as the permanent state.

3. **Round-trip synthesis assertion hardening (Rec 8).** I recommend replacing `"Synthesis" in synthesis` with an assertion anchored to the known `fake_output` fixture content ("# Synthesis\nThe verdict is..."). I am flexible on whether this is a verbatim equality check or a substring check anchored to the fixture's deterministic content. What must be preserved: the assertion must catch a truncation or encoding bug in the `persist_deliberation → read_deliberation_file` path, which the current substring-presence checks do not do. The specific assertion form (exact match vs. anchored substring) is a secondary concern.