### Dangerous Contradictions

- **Step 14 verdict: accurate PARTIALLY DONE marker vs. structurally misleading framing**
  - **sub-step-skeptic claims**: "This is not 'step 14 one-third done' — it is the human-facing piece done and the CI-facing pieces absent." The skeptic argues the three sub-items form a coherent CI/CD consumer contract, not parallel tracks, and that treating persistence-round-trip as a standalone delivery creates a false partial-completion state (Off-Base Assumptions, "Step 14's three sub-items are independently closeable").
  - **spec-061-completeness-auditor claims**: Step 14 verdict is MATCH — "PARTIALLY DONE marker accurate; API-level done, CLI surface and combinatorial matrix correctly identified as open" (Summary Verdicts table).
  - **Why this is dangerous**: If synthesis adopts the completeness-auditor's MATCH verdict without qualification, it endorses the PARTIALLY DONE framing as adequate. A contributor who sees MATCH and PARTIALLY DONE alongside a note that the exit-code sub-item is "blocked on spec 048" may close step 14 when spec 048 lands — leaving the combinatorial matrix permanently deferred. The skeptic's analysis (Rec 8) specifically predicts this failure path. If synthesis adopts the skeptic's framing instead, it requires documentation clarifying that the combinatorial matrix is the core CI/CD coverage deliverable, not an optional extra.
  - **Suggested resolution**: The completeness-auditor's MATCH verdict was based on whether the PARTIALLY DONE annotation was internally consistent with what shipped. The skeptic's critique is that the annotation doesn't communicate the structural dependency correctly. Both observations can simultaneously be true. The resolution is to upgrade the completeness-auditor's verdict from MATCH to REVISE, adding the skeptic's Rec 8 (document the CI/CD consumer contract interdependence) as a required fix. The completeness-auditor should yield on the verdict classification.

- **CI marker taxonomy: Alignment claim directly contradicts Missed Opportunities finding**
  - **sub-step-skeptic claims**: "The marker taxonomy the spec codified is already violated by the first PR that uses @pytest.mark.integration" — Rec 2, P1. The smoke tier runs `pytest engine/tests/ -x -q --timeout=30` with no marker filter, and `TestPersistListShowRoundTrip` is `@pytest.mark.integration` (Missed Opportunities, "Integration-marked tests run in the smoke CI tier").
  - **spec-061-completeness-auditor claims**: In the Alignment section for Step 10 — "Three-tier cost partitioning matches §4.3's marker taxonomy — Smoke / quality / deepeval tiers map to `pytest.mark.smoke` / `pytest.mark.eval` / `pytest.mark.live` respectively." Then, in Missed Opportunities — "Integration-marked tests run in the smoke CI tier... the marker taxonomy the spec codified is already violated... Impact: high."
  - **Why this is dangerous**: The completeness-auditor's review sends contradictory signals within a single section. The Alignment claim says the taxonomy is correctly implemented; the Missed Opportunities finding says it is violated. If synthesis reads only the Alignment claim (as a reviewer scanning the Step 10 REVISE verdict might), it concludes the three-tier structure is correctly implemented. The skeptic's P1 priority leaves no such ambiguity. The contradiction weakens the signal at exactly the point where the signal needs to be strongest — the first PR that defines what @pytest.mark.integration means set the wrong precedent.
  - **Suggested resolution**: The completeness-auditor's Alignment claim is wrong, not the Missed Opportunities finding. The tier structure is correctly *designed* in evals.yml but incorrectly *enforced* — the smoke job has no `-m` filter. The Alignment section should be corrected to remove the taxonomy-matching claim or narrow it to "the tier structure is correctly designed; enforcement is missing." The skeptic's P1 classification is the accurate one. The completeness-auditor should yield on the Alignment framing.

- **Node version fix priority: P1 vs. P2**
  - **sub-step-skeptic claims**: Rec 7 assigns Priority P2 to correcting the Node 18 spec claim to match evals.yml's Node 22 reality. Risk cited: "A future contributor downgrades the CI node version 'to match spec,' introducing a breaking change in the eval pipeline."
  - **spec-061-completeness-auditor claims**: Rec 3 assigns Priority P1 to the same fix, citing Principle XIV (Spec-Implementation Parity): "A spec that says X when the implementation does Y is a bug in the spec." Identical risk description, identical fix.
  - **Why this is dangerous**: If synthesis picks P2, the Node version fix gets deprioritized relative to P1 gaps in both reviews. The risk the two reviews describe identically is not trivial: the spec's §7 step 10 DONE annotation is where a contributor setting up a new CI job would look for runner requirements. Node 18 reached EOL April 2025; a contributor following the spec would introduce a security exposure and potential runtime breakage. The two reviews agree on everything except the urgency label, and the priority label is what determines sequencing.
  - **Suggested resolution**: Both reviews describe the same failure mode with the same words. Defaulting to the higher priority (P1) is conservative and costs nothing. The completeness-auditor's Principle XIV framing provides the constitutional grounding; the skeptic's context ("establishes a pattern of spec-implementation drift in the eval suite itself") provides the broader significance. The skeptic should yield on the priority classification.

---

### Tensions

- **Scope of audit: individual step completeness vs. execution order correctness**
  - **sub-step-skeptic's position**: The orchestrator's skip of steps 5–8 while completing 9, 10, 13, 14a creates a false health signal — "a CI that reports 'smoke tests pass' while G12 is open is a false health signal" (Executive Summary). Recs 1 and 2 are P1, framed as macro-level correctives: fix the §7 header, block steps 7–8 on step 6.
  - **spec-061-completeness-auditor's position**: The audit asks whether the DONE markers for steps 9, 10, 13, 14a accurately reflect what was built — not whether those steps should have been done in this order. Two of four verdicts are REVISE, not REJECT; the work was substantively done but leaves specific gaps requiring follow-up issues.
  - **Nature of tension**: Both audits are correct within their scopes. The completeness-auditor asks "did the orchestrator build what the spec said at step 9?" The skeptic asks "should the orchestrator have been at step 9 at all?" The completeness-auditor could give every step a MATCH verdict and the skeptic's critique would still stand; the skeptic could document every sequencing failure and the completeness-auditor's P1 §3.1.9 gaps would still require filing.
  - **Coordination needed**: Synthesis must draw from both scopes and layer them explicitly. The skeptic's §7 header rewrite (Rec 1) is the framing correction that describes the macro state. Completeness-auditor's per-step REVISE items (Recs 1–6) are implementation-level follow-up issues that live inside the corrected framing. Neither set of recommendations is rendered redundant by the other.

- **G12 "silently rejects providers" as a prerequisite to test-suite quality**
  - **sub-step-skeptic's position**: G12 (VALID_PROVIDERS hardcoded to anthropic/openai) means YAML-configured runs reject 11 of 13 providers at parse time. "Any quality baseline captured before step 6 closes will reflect a broken provider matrix" (Missed Opportunities). Recs 1 and 4 assign P1 to blocking steps 7–8 on step 6 completion.
  - **spec-061-completeness-auditor's position**: Step 13 REVISE focuses on behavioral test gaps — cost estimate 3× and influence output headings (Recs 1–2, both P1) — without noting that these tests run against a provider matrix where YAML-configured runs are partially broken. The completeness-auditor implicitly treats the existing test suite as a valid baseline.
  - **Nature of tension**: The completeness-auditor's per-step gaps are real, but their sequencing relative to G12 matters. Filing "add iterations=3 cost estimate test" as P1 is correct — but if the test is written against the YAML-configured path, G12 will cause it to fail for reasons unrelated to the cost estimate logic. The two P1 sets point at the same timeframe without explicitly acknowledging this interaction.
  - **Coordination needed**: Completeness-auditor's Recs 1 and 2 both use the mock provider via `test_sdk.py` and `test_phases.py` respectively — neither hits the YAML-configured path that triggers G12. The conflict is therefore less severe than it appears. Synthesis should note this explicitly: the two P1 tracks can proceed in parallel because they exercise different code paths. The skeptic's G12 fix is prerequisite only for quality-tier and YAML-configured integration tests, not for the mock-provider pipeline unit tests the completeness-auditor recommends.

- **Step 10 PR attribution: factual gap in completeness-auditor's analysis**
  - **sub-step-skeptic's position**: Rec 6 (P2) notes that PR #83 established the smoke tier; PR #101 added only the quality and deepeval tiers plus `workflow_dispatch` inputs. The step 10 DONE annotation credits PR #101 with the "three-tier" workflow, misattributing the smoke tier's provenance.
  - **spec-061-completeness-auditor's position**: The Step 10 REVISE verdict covers Node version and API key scope. It does not question the PR attribution narrative; the three-tier credit to PR #101 is accepted at face value throughout the review.
  - **Nature of tension**: This is a coverage gap in the completeness-auditor's Step 10 analysis, not a direct contradiction. The skeptic found a factual error the completeness-auditor did not look for. If synthesis relies only on the completeness-auditor's Step 10 REVISE items, the attribution error will survive into the corrected spec annotation.
  - **Coordination needed**: Synthesis should adopt the skeptic's Rec 6 as an additional Step 10 fix item, layered alongside the completeness-auditor's Node version and API key items. The completeness-auditor's Step 10 REVISE verdict remains valid; the skeptic adds a third correction to the same step.

- **§3.1.9 coverage gaps: both P1, but ordered differently relative to G12**
  - **sub-step-skeptic's position**: The skeptic does not audit §3.1.9 at the test-by-test granularity; its concern is the macro sequencing — steps 7 and 8 must not run before step 6. The skeptic would classify adding §3.1.9 tests before fixing G12 as producing misleading baselines.
  - **spec-061-completeness-auditor's position**: Recs 1 and 2 (both P1) call for filing specific §3.1.9 follow-up issues — iterations=3 cost estimate assertion and arbiter influence output heading test — independent of G12 status.
  - **Nature of tension**: The skeptic's sequencing logic would deprioritize completeness-auditor's Recs 1–2 until step 6 closes, because any test of multi-round/arbiter behavior using a YAML-configured path will interact with G12. The completeness-auditor's Recs use mock provider in `test_sdk.py` and `test_phases.py`, which bypass G12. This ordering tension is real but resolvable.
  - **Coordination needed**: Synthesis should explicitly note that the completeness-auditor's §3.1.9 gap-filling tests are mock-provider unit tests parallelizable with G12 remediation. The skeptic's blocking logic applies to quality-tier calibration (step 7) and baseline snapshots (step 8), not to mock-provider behavioral unit tests. Both sets of P1 recommendations can proceed simultaneously.

---

### Safe Agreements

- **Node 18→22 requires immediate spec correction (Principle XIV)**
  - **Shared position**: sub-step-skeptic Rec 7 and spec-061-completeness-auditor Rec 3 independently conclude that the Node version discrepancy must be corrected in the spec to match evals.yml. Both cite the identical failure mode: a future contributor reading §7 step 10's runner requirements would pin Node 18, restoring EOL behavior and breaking the eval pipeline. Both cite Principle XIV.
  - **Combined evidence**: The completeness-auditor verified the specific file location (evals.yml `node-version: '22'` in both the smoke and quality jobs) and the spec location (§7 step 10 "runner requirements: Python 3.12, Node 18"). The skeptic adds broader significance: this discrepancy "establishes a pattern of spec-vs-implementation drift in the eval suite itself" — the very suite designed to prevent such drift. Together, the reviews document the specific fix and its architectural irony.
  - **Confidence level**: High. This is a straightforward factual discrepancy with an unambiguous fix, independently verified by two reviews through different analytical paths (per-step completeness audit vs. macro execution-order audit).

- **CLI persist→list→show is a gap requiring a tracking issue**
  - **Shared position**: sub-step-skeptic Rec 6 (P2) and spec-061-completeness-auditor Rec 6 (P2) independently identify that PR #106's round-trip tests operate at the module-API level (`persist_deliberation`, `list_deliberations`, `read_deliberation_file` called directly) and do not exercise the CLI commands a user actually invokes. Both recommend filing a tracking issue. Both propose the same implementation: `subprocess.run(["uv", "run", "conversus", "list", ...])` and `subprocess.run(["uv", "run", "conversus", "show", slug])` tests.
  - **Combined evidence**: The completeness-auditor verified the test class structure — `TestPersistListShowRoundTrip` calls module functions, not CLI entry points, meaning argument-parsing regressions in the CLI handlers are invisible. The skeptic adds the spec obligation: §3.1.3 lists CLI commands as outside-in surface tests, making this a spec compliance gap in addition to a coverage gap. The two evidence chains are complementary — one from the test class inspection, one from the spec reference.
  - **Confidence level**: High. Both reviews independently identified the same gap, the same specific test class, the same fix, and the same priority. The combined weight of two independent P2 designations should register as a firm recommendation.

- **CI smoke tier's missing -m marker filter undermines the §4.3 taxonomy contract**
  - **Shared position**: sub-step-skeptic (Rec 2, P1) and spec-061-completeness-auditor (Step 10 Missed Opportunities, high impact) both identify that `pytest engine/tests/ -x -q --timeout=30` without a `-m` filter causes `@pytest.mark.integration` tests to execute on every push, violating §4.3's explicit separation of smoke from integration. Both cite `TestPersistListShowRoundTrip` as the specific class now running in the wrong tier.
  - **Combined evidence**: The completeness-auditor documents the specific class and its `@pytest.mark.integration` decorator. The skeptic adds the forward-looking consequence: once integration tests accumulate in the smoke tier, contributors stop trusting the marker taxonomy — future `@pytest.mark.integration` annotations are applied inconsistently, and the taxonomy contract the spec codified is abandoned in practice. The completeness-auditor provides the current violation; the skeptic provides the erosion trajectory. Together, the reviews make a strong case for the fix (add `-m "not integration and not live and not eval"` to the smoke job) as the highest-priority CI change.
  - **Confidence level**: High. Independent verification from two reviews, specific test class named, specific fix stated, §4.3 reference confirmed. Despite the contradiction noted above (completeness-auditor's Alignment section claiming the taxonomy matches), the Missed Opportunities finding is the accurate one and both reviews converge on it.

- **Step 13's §3.1.9 arbiter influence row is not closed at the pipeline output level**
  - **Shared position**: Neither review considers step 13's §3.1.9 coverage complete. The completeness-auditor (Recs 2 and 4, both P1) identifies that `TestBuildArbitrationContextInfluenceLevel` in `test_templates.py` verifies context population but not rendered output, and that `_make_arbiter_config` in `test_phases.py` never sets `influence` (defaults to "binding" for all three trigger tests). While the skeptic does not audit §3.1.9 at this granularity, its characterization of the CI as producing false health signals (Executive Summary) is precisely the condition this gap creates: all arbitration tests pass while a heading-level rendering regression across influence levels goes undetected.
  - **Combined evidence**: The completeness-auditor provides the specific test evidence — `_make_arbiter_config` at lines 907–916 with no `influence` argument, and `TestInfluenceFieldParsed` covering parsing but not rendering. The skeptic provides the contextual significance: the eval suite's purpose is to close gaps between surfaces; an influence-level heading regression in `resolution.md` is exactly the class of silent failure the suite was built to catch. A synthesizer reading `resolution.md` with a binding label when the config specifies advisory has semantic downstream consequences the test suite currently cannot detect.
  - **Confidence level**: Medium. The completeness-auditor's contribution is direct (specific test inspection); the skeptic's contribution is inferential (the false-health-signal framing applies to this gap without naming it). The logical chain is sound but the inference requires the synthesis to accept that "false health signal" applies to the influence gap specifically.