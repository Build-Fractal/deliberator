### Cross-Review of pr-evidence-grounding's Phase 1 Review

I read pr-evidence-grounding's review with appreciation for the depth of code grounding (`construction.py:139-171` for `GapFiller`, the `SourceProvenance.filled_by` enum, the explicit "no `optimizer/`/`nashopt/`/`ampl/` directories exist" check). That codebase grounding is something my own wording-only review cannot supply, and several of pr-evidence-grounding's findings sharpen and partially refute mine. The evaluation below is structured per the brief.

### Dangerous Contradictions

1. **Mechanism of pinning: runtime cache vs. on-disk artifact.** pr-evidence-grounding's review (Off-Base #1, Recommendation #2) claims the candidate's "cached values from the first resolution are reused" describes a non-existent runtime cache, and that the actual mechanism in the codebase is artifact-mediated (`objective.yml` written once, re-read on subsequent runs). My own review (Missed Opportunity at L465-466, Recommendation #6) treats the cache as if its location is *unspecified* and recommends "persisted to the run's output directory" as a generic fix — which happens to converge with pr-evidence-grounding's specific `objective.yml` pointer, but I framed it as a wording precision gap rather than a description-vs-reality contradiction. **Why dangerous**: if the constitution says "cached values" and the code says "re-read `objective.yml`," a future reviewer auditing a PR can't tell whether an in-memory cache is sufficient (consistent with my "specify the storage" framing) or insufficient (consistent with pr-evidence-grounding's "the artifact IS the mechanism" framing). The two reviews push toward different fixes. **Suggested resolution**: adopt pr-evidence-grounding's specific anchor — replace "cached values from the first resolution are reused" with "values are persisted to `objective.yml` (spec 014 FR-012) and re-loaded rather than re-resolved" — because it both addresses my generic precision concern and fixes pr-evidence-grounding's grounding concern in one edit.

2. **Whether the principle is descriptive-of-reality or normative-target.** pr-evidence-grounding (Executive Summary, Off-Base #4) characterizes XVI as describing **a target state the codebase has not yet reached**, written in the indicative mood, and recommends an "evidence-pending" hedge (Recommendation P1 #1). My review (Off-Base #1, Recommendation P1 #1) treats the descriptive prose as the *wording* problem and recommends converting it to MUSTs — i.e., upgrade the verb, the discipline already exists, just say so normatively. **Why dangerous**: pr-evidence-grounding says the descriptive prose is silently false because the implementation isn't there yet; I say the prose is descriptive when it should be obligatory. If pr-evidence-grounding is right, my "promote to MUST" recommendation would lock in a constitutional obligation the code does not yet satisfy — converting a documentation drift into a constitutional-violation backlog. **Suggested resolution**: do both, sequenced. First perform spec 068 §7 Q1's required grep audit (pr-evidence-grounding's P1 #1). If the discipline is implemented, my MUST conversion is safe. If not, add the "evidence-pending" hedge first and convert to MUST in a follow-up amendment after the implementation lands. The two reviews are not actually opposed — they are sequenced.

3. **Whether spec 014 FR-012 is the right anchor.** pr-evidence-grounding (Missed Opportunity #2, Recommendation P2 #4) says the candidate cites spec 013 but should cite spec 014 FR-012/SC-004 because that's where deterministic assembly is contracted and tested. My review (Missed Opportunity at L460-462, Recommendation P2 #8) recommends citing "a spec FR for the pinning behavior" generically and points at spec 013 in passing. **Why dangerous**: my review misidentifies the locking spec. Spec 013 defines the *templates*; spec 014 defines the *assembly determinism contract*. Anchoring a normative wording fix to the wrong spec FR would make the citation un-auditable. **Suggested resolution**: defer to pr-evidence-grounding here — cite spec 014 FR-012 for stage 3 determinism, spec 014 FR-020/FR-021 for the artifact contract, and spec 013 only as the source of the template definitions stage 1 parses.

### Tensions

1. **VII ↔ XVI reconciliation: same diagnosis, different cuts at the fix.** Both reviews flag the VII vs. XVI cross-run determinism contradiction (my Off-Base #2 / Recommendation #3; pr-evidence-grounding's Missed Opportunity #9 and "Cross-run variance is acceptable deserves a note about VII reconciliation"). My fix is to add an explicit carve-out in the v2.3.2 clarification ("This carves an explicit exception to Principle VII…"). pr-evidence-grounding's framing is lighter — "acknowledge the apparent conflict" rather than codify an exception. The tension is pr-evidence-grounding may consider VII unchanged-by-XVI given the artifact-mediated mechanism (since `objective.yml` is the same input across runs, VII is satisfied), whereas I treat user inputs (not `objective.yml`) as VII's "same inputs" antecedent. We agree on the diagnosis; we disagree on whether a constitutional carve-out or a clarifying cross-reference is the right precision fix.

2. **Cross-run variance bounds.** My review (Recommendation #5) wants explicit bounds: "cross-run variance in resolved parameter *values* is acceptable; cross-run variance in the assembled objective function's *shape* is prohibited." pr-evidence-grounding's review doesn't push for bounds — it pushes for naming the failure mode ("Two `/conversus mode` invocations on the same problem.md MAY produce different `objective.yml` files; the constitution permits this"). Both are precision wins, but they aim at different ambiguities. Mine narrows what variance is acceptable; pr-evidence-grounding's widens what variance is acceptable but names where it lands. Reconcilable: combine them into "cross-run variance in resolved parameter values is permitted (e.g., two `/conversus mode` invocations on the same problem.md MAY produce different `objective.yml` files); cross-run variance in the assembled objective function's shape is prohibited."

3. **Stage 2 disambiguation: question generation vs. answer extraction.** My review (Recommendation #7) flags that stage 2 collapses two distinct LLM activities and recommends an inline "(a) translating gaps into questions, (b) extracting parameter values from answers" expansion. pr-evidence-grounding's review does not raise this. Tension is mild: pr-evidence-grounding's grounding (`GapFiller.fill()`) reveals the protocol is a single function call from the principle's perspective, which weakens my disambiguation recommendation — if the codebase treats it as one call, the constitution can too. I'd downgrade my Recommendation #7 from P2 to P3 in light of pr-evidence-grounding's grounding.

4. **Naming the agent that pins parameters.** My review (Recommendation #10) flags passive voice ("are pinned") and recommends naming the orchestrator. pr-evidence-grounding's review identifies the actual hook: `SourceProvenance.filled_by` (Missed Opportunity, "constitutional reference to `SourceProvenance.filled_by`"). Tension: my recommendation names a *responsible component*; pr-evidence-grounding names an *auditable hook*. Both are precision wins, but pr-evidence-grounding's is grounded in a concrete enum that already exists in `construction.py:262-279`. I'd merge: "The run orchestrator MUST pin resolved parameter values; pinning is auditable via `SourceProvenance.filled_by`."

5. **"Mechanical assembly" vocabulary drift.** pr-evidence-grounding (Off-Base #2, Recommendation P1 #3) flags that the constitution says "mechanical assembly" while spec 014 FR-012 and `construction.py:8-14` say "deterministic." My review does not catch this — I praised "bit-for-bit" as unambiguous (Alignment item) without noticing the stage label drifted from the codebase's vocabulary. Tension: pr-evidence-grounding identifies a real precision regression that my wording-only review missed, because I didn't have the codebase in view. Concession: pr-evidence-grounding's recommendation is correct; either rename stage 3 back to "deterministic assembly" or update spec 014 FR-012's vocabulary in lockstep.

### Safe Agreements

1. **The pinning rule is the load-bearing claim and the candidate underspecifies it.** Both reviews independently rank this P1. My review frames it as descriptive-vs-normative; pr-evidence-grounding frames it as text-vs-implementation. Both diagnose the same wording site (L463-466) as the most fragile sentence in v2.3.2.

2. **"Per deliberation run" is undefined and that ambiguity is operational, not stylistic.** My Recommendation P1 #2 ("define deliberation run") and pr-evidence-grounding's Missed Opportunity (""Per deliberation run" is undefined … without an artifact anchor … the within-run-vs-cross-run boundary is ambiguous") converge on the same fix: tie the term to a concrete artifact. Whether that artifact is a session ID, an output directory, or `objective.yml` is a detail; both reviews agree the abstract term must be anchored.

3. **The falsification clause is the candidate's strongest sentence.** My Alignment item ("Falsification clause is concrete") and pr-evidence-grounding's Alignment ("The clarification's testable invariant is well-formed") agree that L496-498 is the part of v2.3.2 that future reviewers can mechanically check against. Both reviews implicitly treat it as the load-bearing escape hatch that lets the rest of the wording be tightened without losing the principle's enforceability.

4. **Spec 014 (not just spec 013) deserves citation, and `objective.yml` is the natural anchor.** pr-evidence-grounding makes this explicit (Missed Opportunity #1, #2). My review converges on it indirectly: my Recommendation #6 wants pinning storage and lifetime named, and my Recommendation #8 wants a spec FR cited. Both reviews agree the constitutional text should be anchored to a specific artifact and a specific FR rather than waving at spec 013 alone.

### Closing Note

pr-evidence-grounding's grounding caught two issues I missed (mechanical-vs-deterministic vocabulary drift; spec 014 not spec 013 as the right anchor) and reframed one issue I had identified (descriptive prose) into a sequenced problem (verify-then-promote, not promote-blindly). Conversely, my review pushed on internal-language precision (MUST/SHOULD verbs, "claims" vs. "requires," bounded "acceptable") in places pr-evidence-grounding's grounding-first lens didn't reach. The two reviews are complementary — neither is sufficient alone, both together cover the wording-and-grounding surface a v2.3.2 merge needs.
