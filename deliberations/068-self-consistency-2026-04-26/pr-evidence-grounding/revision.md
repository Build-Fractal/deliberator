### Recommendation Dispositions

**Rec 1 (P1): Add an "evidence-pending" hedge OR perform spec 068 §7 Q1 grep audit.**
**Hold, with refinement.** Both cross-reviews engaged this. wording-precision (Dangerous Contradictions #2) reframed it usefully: their "promote pinning to MUST" and my "evidence-pending hedge" are not opposed but sequenced — verify first, then promote. cross-principle-coherence's Tension #1 likewise treats their VII-scoping fix as more durable than my hedge. The refined disposition: **the audit must happen before merge** (this is what spec 068 §7 Q1 already requires); the hedge is the fallback if the audit cannot be performed in this iteration. I am not abandoning the recommendation, but I now treat it as the **gating step** for the other recommendations rather than a parallel textual fix.

**Rec 2 (P1): Replace "cached values from the first resolution are reused" with the artifact mechanism.**
**Strengthened.** Both cross-reviews independently endorsed this as the highest-leverage edit. wording-precision (Dangerous Contradictions #1) framed the combined fix sharply: "Resolved parameter values MUST be persisted to `objective.yml` (spec 014 FR-012) for the duration of the run; the LLM MUST NOT be re-invoked for parameter resolution within the same run." That phrasing layers wording-precision's MUST framing on top of my artifact anchor — it addresses both reviews' concerns in one edit and is what should land. cross-principle-coherence's Dangerous Contradictions #1 confirms my "no cache layer" finding undermines its own Principle VII reconciliation framing, which means this edit also retroactively repairs coherence's analysis surface. **Promote to top-priority concrete edit.**

**Rec 3 (P1): Reconcile "mechanical assembly" vs "deterministic assembly" terminology drift.**
**Strengthened, with priority elevation acknowledged.** cross-principle-coherence's Dangerous Contradictions #2 corrected its own original P3 framing on this issue — calling it a "three-way drift" (code: deterministic, spec 014: deterministic, constitution v2.3.2: mechanical) and conceding their P3 should be promoted to P2. wording-precision's Tension #5 also concedes this on grounds that they didn't catch it without code in view. The disposition holds at P1; both cross-reviewers now agree the priority is justified. **Concrete edit**: in XVI bullet 3 (line 471), either rename "Mechanical assembly" to "Deterministic assembly" (preserving spec 014/code vocabulary) OR explicitly note "(also called 'mechanical assembly' in Principle VIII vocabulary)." The first option is cleaner.

**Rec 4 (P2): Cross-reference spec 014 FR-012/SC-004 and spec 016.**
**Hold.** wording-precision's Dangerous Contradictions #3 explicitly defers to me here. cross-principle-coherence's Tension #3 endorses pairing this with Principle XI cross-reference. **Concrete edit**: append to XVI bullet 3 "(spec 014 FR-012, regression-tested by SC-004)" and add a stage-2 reference to "(see spec 016 for the plugin-system surface where future LLM gap-fillers register)."

**Rec 5 (P2): Define "deliberation run" explicitly via concrete artifact.**
**Strengthened.** Both cross-reviews ranked this as a top safe agreement. wording-precision's Tension #2 proposed compatible phrasing ("a single invocation of the run engine producing one output directory; retries within an invocation are part of the same run") that I should adopt — but anchored to `objective.yml` rather than defined-by-fiat, per their resolution suggestion. **Concrete edit**: add a sentence to the v2.3.2 clarification block (line 491-498): "A 'deliberation run' is defined as a single invocation that produces one `objective.yml` artifact; the artifact's existence and content define run boundaries." This is one edit that closes Rec 5 and Rec 2's lingering ambiguity together.

**Rec 6 (P2): Add constitutional reference to `SourceProvenance.filled_by` as auditable hook.**
**Hold, with merge into V/XXIV recommendations from coherence.** cross-principle-coherence's Tension #5 layers this with their Principle V observability recommendation (phase report line surfacing `filled_by` counts). wording-precision's Tension #4 names the same hook from a different angle. **Concrete edit**: add to the v2.3.2 clarification "Pinning is auditable via `SourceProvenance.filled_by` (`conversus/schemas/construction.py`)." Coherence's V cross-reference is a separate but compatible add.

**Rec 7 (P2): Acknowledge optimization-layer specs (016-019) are partly under construction.**
**Hold.** cross-principle-coherence's Dangerous Contradictions #3 reinforced this — their Principle XII (No Dead Infrastructure) lens sharpens the concern: declaring discipline over a runtime that doesn't yet exist potentially codifies sanction for not-yet-built infrastructure, which XII prohibits. wording-precision did not engage this specifically. **Concrete edit**: add to the Origin note (line 500) "Some referenced runtime layers (specs 016-019, optimizer/nashopt/ampl) are spec'd but partly under construction; this principle codifies the discipline they will satisfy when implemented."

**Rec 8 (P3): Add regression test for SC-004.**
**Hold, unchanged.** Neither cross-review pushed back on this, and it is implementation-side rather than constitutional text. Out-of-band action item.

**Rec 9 (P3): Clarify XVI ↔ VII relationship.**
**Refined to combine with wording-precision Rec #5.** wording-precision's Dangerous Contradictions #2 and Tension #4 (cross-run variance bounds) and cross-principle-coherence's Tension #1 converge on a stronger combined fix: name the bound (variance in *values* is permitted; variance in *shape* is prohibited) AND give the concrete user-facing example (two `/conversus mode` invocations on the same problem.md MAY produce different `objective.yml` files). **Concrete edit**: add to the v2.3.2 clarification "Two invocations of the same workflow against the same input MAY produce different `objective.yml` files (cross-run parameter variance is permitted); but within a single run, the assembled objective function's *shape* MUST NOT vary. Principle VII applies to the assembled objective function, not to LLM gap-filling."

**Rec 10 (P3): File follow-up spec for CI lint detecting re-entrant `GapFiller.fill()`.**
**Hold.** cross-principle-coherence's Tension #1 explicitly endorses pairing this with their Principle XXIV cross-reference, since XXIV requires "Contract test reproducing the failure scenario" but no such test exists today. **Concrete action**: file a follow-up spec issue (out of band), and add an XXIV cross-reference inside XVI to anchor the obligation: "(Principle XXIV's contract-test obligation applies to the within-run pinning invariant.)"

### New Recommendations

**New Rec 1 (P1): Add a v2.3.2 Sync Impact Report.**
wording-precision's Scope Creep Check observation (the candidate's top-of-file SIR still describes v2.3.1 even though the version footer says 2.3.2) is a precision/metadata gap I missed. The new SIR is also the natural place to record either the spec 068 §7 Q1 grep result (per my Rec 1) or the explicit "evidence-pending / aspirational" status. **Concrete edit**: prepend a v2.3.2 SIR block above the v2.3.1 SIR, naming the XVI clarification, the verification status of the pinning discipline, and either the audit result or the hedge.

**New Rec 2 (P2): Combine `objective.yml` artifact reference, Principle X "one obvious way to find the result", and Principle XI Single Source of Truth into one anchor sentence.**
cross-principle-coherence's Dangerous Contradictions #3 explicitly merged my "objective.yml as pinning artifact" finding with their "Principle X anchor" recommendation into a single P1 actionable. This is a concrete merge of three separate threads. **Concrete edit**: add a sentence to XVI bullet 2 (line 462-470): "The pinning is artifact-mediated: parameter values are persisted to `objective.yml` (spec 014 FR-012, FR-020/021), which serves as the single authoritative source (Principle XI) for resolved parameters and the obvious location to find the resolved math (Principle X)."

### Position Summary

My original review's central finding — that v2.3.2's pinning rule describes a runtime cache that does not exist in `conversus/schemas/construction.py`, while the actual mechanism is artifact-based persistence via `objective.yml` (spec 014 FR-012) — survived both cross-reviews intact and was **strengthened** by them. wording-precision came at the same fragile sentence (lines 463-466) from a verb-auditing angle and converged on the identical defect. cross-principle-coherence came at it from a textual-coherence angle and conceded that their Principle VII reconciliation framing was built on a phantom cache that my grounding work disproved. Both cross-reviewers explicitly defer to my grounding on the artifact-vs-cache mechanism question. This is the highest-confidence finding across all three reviews and should land as the load-bearing edit in v2.3.2.

The most useful pushback I received was structural sequencing rather than substantive disagreement. wording-precision's Tension #1 framed v2.3.2 as primarily a wording amendment that can land directly, versus my framing as a grounding amendment requiring code audit before merging. Their resolution — do the audit first, then apply tightenings against verified-true facts — is correct, and I have re-pointed Rec 1 to be the gate for the others rather than a parallel hedge. cross-principle-coherence sharpened my "mechanical assembly" terminology drift finding (their Dangerous Contradictions #2 promoted what they originally called P3 to P2 on grounds of three-way drift) and merged my `objective.yml` artifact recommendation with their Principle X/XI cross-reference into a single anchor sentence (New Rec 2). Both moves tighten the recommendation set without softening it.

What I am letting go: my Rec 7 (stage 2 question/extraction split) was already P3 and wording-precision's grounding-aware critique (the `GapFiller.fill()` boundary is one call from the principle's perspective) further weakens it; I would now decline to push for that disambiguation. What I am holding firm on: the principle is currently asserted in the indicative mood about an implementation that is partly aspirational, and merging v2.3.2 without either a verified audit or an honest "evidence-pending" hedge produces a constitution claiming properties of code that does not exist. The combined edit set above — artifact anchor (Rec 2), terminology reconciliation (Rec 3), VII scoping with cross-run example (Rec 9 refined), `SourceProvenance` audit hook (Rec 6), v2.3.2 SIR (New Rec 1), and the unified X/XI/objective.yml anchor (New Rec 2) — fixes the candidate without re-litigating the three-stage taxonomy that all three reviews independently endorsed.
