Reading the target document now before producing the gate-faithfulness review.

I now have the full document. Producing the gate-faithfulness review.

### Executive Summary

The v2.5.0-pre-migration constitution presents three grandfathered principles — VI (Scripts Over Markdown), X (Zen of Python Output), and XVI (Mathematical Transparency) — that the v2.4.0 grandfathering disclosure explicitly flagged as failing the Constitutional Inclusion Criteria gate. The pending v3.0.0 migration proposes removing VI and X from the constitution and applying path (c) to XVI (retain the mechanically-verifiable pinning discipline, relocate the plain-language explanation sub-bullet to operational guidance). Gate-faithfulness audits whether these dispositions are correct and evaluates the strongest plausible contrarian arguments in advance.

The verdicts are sound. VI fails Criterion 2 unambiguously: the operative conditional "when the artifact drives behavior" (line 351) requires semantic judgment that no automated check can substitute for, and the gate text's exact failure template — "If a reviewer must reason 'well, X might be okay if Y'" (lines 1210–1212) — maps directly onto VI's "when it's just for human orientation, markdown is appropriate" carve-out (line 360). X fails Criteria 1 and 2 simultaneously: "readable" (line 487) is the gate's own worked example of a non-mechanical requirement ("code should be readable" — line 1221), with the SIR itself supplying the admission of Criterion 1 failure in the phrase "irreducibly subjective" (line 105). XVI's disposition is the most nuanced: the principle passes the gate on its pinning-discipline content and fails only on the plain-language sub-bullet (lines 694–698), which cannot be automatically verified. Path (c) is the right surgical tool.

The contrarian's strongest play for VI is a load-trigger CI lint — enumerating all markdown files referenced by SKILL.md load triggers and flagging any that exist. This argument has genuine partial validity: it is a real mechanical check for the most common violation pattern. It does not rescue VI from Criterion 2 failure because VI's text covers all artifacts that "drive behavior," not only load-trigger-referenced files, and the exception clause at lines 359–362 ("when a markdown artifact exists purely for human orientation, markdown is appropriate") requires semantic judgment that the lint cannot make. The contrarian's insight belongs in the audit trail as a constructive finding, not a reversal of the migration verdict.

**Most important recommendation**: when VI and X are migrated, the receiving operational guidance documents MUST include the specific, falsifiable sub-requirements that the principles contain (VI's load-trigger prohibition and X's `summary/final.md` structural requirement) so that partial mechanical enforcement survives the migration. A migration that moves general principles to operational guidance and discards the falsifiable sub-requirements is not a clean reclassification — it is a silent capability downgrade.

---

### Alignment

- **Criterion 2 kills VI via exact template match** (Principle VI, lines 349–362; gate Criterion 2, lines 1208–1212): The gate states: "If a reviewer must reason 'well, X might be okay if Y,' the principle is too vague." VI's structure IS this form: the operative condition is "when the artifact drives behavior" (line 351), and the explicit carve-out at lines 359–362 forces exactly that reasoning: "when a markdown artifact exists purely for human orientation... markdown is appropriate." Every application of VI requires a reviewer to first classify the artifact as behavior-driving vs. human-orientation, a classification the principle's text does not make computable. The gate's failure template and VI's operative structure are isomorphic.

- **Grandfathering disclosure self-identifies all three failures** (SIR v2.3.2→v2.4.0, lines 102–111): The disclosure explicitly labels each failure mode: VI as "judgment-laden 'when the artifact drives behavior'"; X as "irreducibly subjective 'readability counts'"; XVI as "plain-language explanation requirement is unverifiable." These labels map precisely to Criterion 2 (judgment-laden = unfalsifiable scope), Criterion 1 (irreducibly subjective = no automated check feasible), and Criterion 2 again (unverifiable = no mechanical check). The constitution's own amendment record corroborates the migration verdicts before any deliberation begins.

- **Gate worked example directly condemns X** (gate Criterion 1 worked example, lines 1220–1222): The gate's primary worked example for Criterion 1 failure is: "A principle proposing 'code should be readable' fails Criterion 1: no automated check is feasible." Principle X at lines 486–487 states: "Output MUST be clean, readable, and unsurprising... readability counts." X is not analogous to the worked example — it IS the worked example, applied to output rather than code. The gate authors either had X in mind when writing the example or independently converged on the same archetype; either way, the Criterion 1 failure is as close to explicit as a grandfathering finding can be.

- **XVI's pinning discipline provides a sketchable Criterion 1 path** (Principle XVI, lines 742–746; SIR v2.3.1→v2.3.2, lines 178–181): The principle's own Enforcement bullet (lines 742–746) names the mechanical check: "a CI lint detecting re-entrant GapFiller.fill() calls." The SIR follow-up TODO at line 181 confirms the path is concrete: "CI lint detecting re-entrant GapFiller.fill() per spec 068 follow-up." Gate Criterion 1 requires only that "the path to building [the check] MUST be concrete enough that an engineer reading the principle can sketch the check in one paragraph." The Falsification clause at line 747 provides that sketch: "A future PR that re-resolves parameters mid-deliberation... violates this principle." Path (c) correctly preserves this content at constitutional level.

- **XVI's plain-language sub-bullet is the only Criterion 2 failure** (Principle XVI, lines 694–698): The plain-language requirement specifies the required form of an explanation: "'87% of agents are at their best possible position given others' positions' is required." No mechanical check can determine whether a given explanation satisfies this criterion — a lint can detect the presence of a numerical output, but cannot assess whether the accompanying text constitutes an adequate plain-language explanation. This sub-bullet is the only clause in XVI that fails the gate; path (c) correctly identifies it as the migration target rather than treating XVI as wholly non-conformant.

---

### Missed Opportunities

- **Contrarian's load-trigger CI lint has genuine partial validity**: The contrarian will argue that "drives behavior" (line 351) is operationalized by SKILL.md load triggers — any markdown file appearing in a load trigger is definitionally a behavior-driving artifact. A lint `grep -E 'read|load|references/' SKILL.md | grep '\.md'` would detect violations in the most common failure mode. This is a real Criterion 1 sketch, and gate-faithfulness should acknowledge it rather than dismiss it. The reason it does not rescue VI is that VI's text covers a broader scope than load-trigger-referenced files: "consumed by automation or agents to make decisions" (line 353) includes any markdown file an agent reads by convention, not only files named in explicit triggers. The exception clause at lines 359–362 additionally requires judging the purpose of a file, which the lint cannot do. But the load-trigger lint is a valid partial check that SHOULD be carried into the receiving operational guidance document as a concrete enforcement mechanism. Omitting it from the migration would discard a useful tool. Impact: high.

- **X's `summary/final.md` sub-bullet is mechanically checkable and uses SHOULD, not MUST**: Line 490 states: "There SHOULD be one obvious way to find the result. The output tree follows a predictable structure: `summary/final.md` is always the starting point." A CI check that verifies `summary/final.md` exists after any `/conversus run` completes would enforce this. The SHOULD weakener and the "always the starting point" language make this a SHOULD in the constitution. During migration, this sub-requirement SHOULD be promoted to MUST in the receiving operational guidance document and paired with a concrete CI check. If this promotion does not happen, the one mechanically checkable requirement in X is lost without gaining any enforcement improvement. Impact: high.

- **The MUST→SHOULD framing is analytically incorrect for VI**: The review task asks whether migration "smuggled a strength reduction" by converting MUST to SHOULD. VI already uses SHOULD at line 353: "it SHOULD be a script, config, or structured data." The operative condition "when the artifact drives behavior" makes the SHOULD unenforceable as a constitutional requirement — not because SHOULD is weak, but because the condition that triggers the SHOULD is unfalsifiable. If the v3.0.0 SIR characterizes the migration rationale as "MUST→SHOULD downgrade," the audit trail is misleading. The correct characterization is: VI's scope-qualifier failure makes even the existing SHOULD unenforceable at constitutional level. The strength of the prescription is not the issue; the falsifiability of the scope is. Future reversal arguments will incorrectly cite "no strength was lost" as grounds for restoration if this framing error enters the audit record. Impact: medium.

- **Path (c) is undefined in the pre-migration document**: The review task defends "path (c)" for XVI, but the pre-migration document contains no taxonomy of migration paths. If the v3.0.0 SIR refers to "path (c)" without defining what paths (a), (b), and (c) are, the audit trail contains an unresolved reference. Specifically: a future reader of the SIR cannot determine whether XVI's refactoring was a close call between paths or an obvious fit for one option. The path taxonomy MUST be defined in the SIR itself. Impact: medium.

- **Asymmetry test for IX must be addressed proactively**: The contrarian's strongest asymmetry argument is that IX's "prefer pure functions over classes" (line 411) requires the same semantic judgment as VI's "when the artifact drives behavior." Both require classifying an artifact — a class vs. a function, or a markdown file as behavior-driving vs. human-orientation. The response that saves IX from the asymmetry argument is structural: IX's "prefer" clause is accompanied by a large block of entirely mechanical MUSTs at lines 433–456 (explicit type annotations on ALL signatures, Pydantic models for ALL data structures, StrEnum for ALL closed behavioral choices). A PR violating any of these MUSTs fails Criterion 1 via mypy, pyright, or grep. The "prefer" clause is not the only content in IX — it is the soft framing for a principle whose enforcement weight rests on the mechanical MUST clauses. VI has no such mechanical MUST block accompanying its judgment-laden conditional. If this asymmetry response is not explicitly stated in the v3.0.0 SIR, the contrarian argument survives in the audit record as unaddressed. Impact: high.

- **XV's "MUST NOT block" is architecturally enforced, not merely stated**: The contrarian may argue XV's "Plugin failure MUST NOT block core execution" (line 627) requires semantic judgment about what constitutes "blocking." The correct response: XV's clause at lines 637–639 ("The plugin interface enforces this — `PluginResult` has no mechanism to modify core state") converts what would be a judgment-laden statement into an architectural invariant. The enforcement is structural: `PluginResult` is a typed interface without core-state mutation methods. A test that injects a crashing plugin and asserts `PluginResult` isolation satisfies Criterion 1 concretely. This is the structural property that distinguishes XV from VI: XV names the enforcement mechanism; VI does not. Impact: medium (this finding prevents an incorrect symmetry argument from appearing in cross-review).

- **XVI's constitutional remainder must be explicitly bounded**: Path (c) migrates the plain-language sub-bullet at lines 694–698 and retains the rest of XVI. But "the rest" is a large body of text spanning lines 655–758. If the v3.0.0 SIR does not explicitly mark which clauses remain constitutional after path (c), future reviewers will not know which XVI requirements carry constitutional force vs. which have been relocated. The SIR MUST include a clause-by-clause retention/migration map for XVI. Impact: high.

---

### Off-Base Assumptions

- **"Path (c) = refactor XVI" assumes a defined taxonomy that does not exist in the pre-migration document**: The review instructs gate-faithfulness to defend "XVI was correctly refactored under path (c)." The constitution document contains no definition of paths (a), (b), or (c) — these appear to be deliberation-local terms from the review prompt itself, not terms defined in the target document. The correct framing: the gate-faithfulness review should defend the specific disposition (surgical clause migration for XVI) without assuming a pre-defined taxonomy that future readers will need to reconstruct from deliberation artifacts. The disposition is defensible on its merits; it does not require a named path to be sound.

- **Asymmetry test framing assumes IX/XXIV fail on the same Criterion 2 ground as VI**: The review task asks whether "VI fails Criterion 2 on a ground that XV/XXIV/IX also fail on." This framing assumes the failure ground is identical. It is not. The v2.4.0 gate's worked example ("code should be readable" — line 1221) identifies Criterion 1 failure as the failure mode for purely subjective requirements. VI's Criterion 2 failure is structurally different: VI has a specific, falsifiable prescription (use scripts, not markdown) but an unfalsifiable scope-qualifier that determines when the prescription applies. IX/XXIV have falsifiable scope-qualifiers (or no qualifier at all) because their scope is defined by enumeration or computable structure. The asymmetry test's premise that these principles "fail on the same ground" is off-base; the relevant question is whether the scope-qualifier failure type generalizes, which it does not to IX/XXIV/XV for the structural reasons elaborated in Missed Opportunities above.

- **The MUST→SHOULD framing assumes VI currently uses MUST**: The review task asks whether the migration "smuggled a MUST→SHOULD strength reduction." VI at line 353 already uses SHOULD. There is no MUST to reduce. Any characterization of the migration as a strength reduction would be factually incorrect for VI. For X, the headline (line 485) uses MUST: "Output MUST be clean, readable, and unsurprising." If the migration converts this to a SHOULD in operational guidance, that IS a strength reduction — but it is a reduction that the principle itself invited by pairing MUST with an unenforceable adjective ("readable"). The MUST→SHOULD analysis applies to X but not to VI, and the distinction matters for the audit trail.

---

### Actionable Recommendations

1. **Include load-trigger CI lint in VI's receiving operational guidance document** (Priority: P1)
   - **Current state**: Principle VI at lines 349–362 contains the general prescription "when the artifact drives behavior, prefer scripts." The migration removes this from constitutional status. No mechanical check is specified for the receiving document.
   - **Proposed change**: The operational guidance document receiving VI's content MUST include a concrete, falsifiable rule: "Any file referenced by a SKILL.md load trigger or config-conditional trigger MUST NOT be a freeform markdown file unless explicitly annotated as human-orientation documentation in the load trigger comment." This rule is mechanically checkable via the load-trigger lint the contrarian correctly sketched.
   - **Rationale**: The contrarian's CI lint is a valid partial check. Discarding it during migration downgrades enforcement without a compensating benefit. Operational guidance can carry mechanically checkable requirements even when the general principle that motivated them fails the constitutional gate.
   - **Risk if ignored**: The most common violation pattern (markdown file appearing in an agent load trigger) goes undetected after migration. The migration becomes a net enforcement downgrade rather than a clean reclassification.

2. **Promote X's `summary/final.md` requirement to MUST in receiving operational guidance** (Priority: P1)
   - **Current state**: Principle X line 490 states "There SHOULD be one obvious way to find the result. The output tree follows a predictable structure: `summary/final.md` is always the starting point." SHOULD weakener; no CI check.
   - **Proposed change**: The operational guidance document receiving X's content MUST include: "After any `/conversus run` completes, `summary/final.md` MUST exist. CI MUST fail if this file is absent." Remove the SHOULD weakener. Pair with a structural output assertion in the run engine tests.
   - **Rationale**: This is the one mechanically checkable requirement in X. Migration SHOULD preserve it at full normative force. A SHOULD in the constitution becoming an advisory note in operational guidance is a silent downgrade — the gate's purpose is reclassification, not dilution.
   - **Risk if ignored**: The output structure guarantee that makes X's text partially enforceable is lost. Future deliberation runs may produce output without `summary/final.md` without triggering any check.

3. **Define path taxonomy explicitly in the v3.0.0 SIR** (Priority: P1)
   - **Current state**: The deliberation references "path (c)" for XVI's disposition, but no taxonomy of migration paths is defined in the pre-migration document or the Governance section.
   - **Proposed change**: The v3.0.0 SIR MUST define the taxonomy before applying it: (a) full migration to operational guidance — the principle's entire content relocates and loses constitutional force; (b) full retention — the principle passes the gate on current wording or is amended to pass; (c) surgical clause migration — the mechanically-verifiable body is retained at constitutional level; the non-conformant clause(s) are relocated to operational guidance with an explicit cross-reference from the retained text.
   - **Rationale**: An audit trail finding of "correctly refactored under path (c)" cannot be verified by a future reader without knowing what path (c) means. The taxonomy is the load-bearing structure of the migration decision.
   - **Risk if ignored**: The XVI disposition becomes unreproducible. A future reviewer cannot determine which XVI clauses retain constitutional force without re-running the deliberation.

4. **Add clause-by-clause retention/migration map for XVI in the v3.0.0 SIR** (Priority: P1)
   - **Current state**: XVI spans lines 655–758 and contains a mix of passing content (pinning discipline, GapFiller.fill() boundary, objective.yml persistence) and failing content (plain-language explanation sub-bullet at lines 694–698). The boundary between retained and migrated content is not marked.
   - **Proposed change**: The v3.0.0 SIR MUST include an explicit table mapping each XVI clause (by sub-bullet or named block) to its disposition: RETAINED (constitutional), MIGRATED (operational guidance — receiving document named), or UNAFFECTED (clarification text exempt from the gate). The clause at lines 694–698 is the only MIGRATED entry; all others are RETAINED.
   - **Rationale**: Without this map, future amendments to XVI will not know which parts can be modified as constitutional requirements. The SIR's audit trail is only useful if it specifies what changed and where.
   - **Risk if ignored**: Future amendments to XVI inadvertently modify the operational guidance boundary without triggering a constitutional amendment, and future reviewers apply constitutional weight to the migrated plain-language clause.

5. **Address the IX asymmetry argument explicitly in the v3.0.0 SIR** (Priority: P1)
   - **Current state**: The SIR does not preemptively address the structural distinction between VI's scope-qualifier failure and IX's judgment-laden "prefer" clauses. The contrarian's asymmetry argument (if VI fails, IX should too) will arise and has no documented response in the audit trail.
   - **Proposed change**: The v3.0.0 SIR MUST include an "Asymmetry Resolution" section stating: "VI's operative condition is the ONLY content determining when the principle's prescription applies, and that condition is unfalsifiable. IX's 'prefer' clauses accompany a large block of mechanical MUST requirements (explicit typing, StrEnum discipline, Pydantic enforcement) that independently satisfy Criterion 1. The 'prefer' framing in IX is soft guidance preceding enforceable requirements; the 'when it drives behavior' framing in VI is the only trigger condition for its sole prescription. These are structurally different, not merely different in degree."
   - **Rationale**: An unaddressed asymmetry argument in the audit trail is an open attack surface for the migration's reversal. The SIR must close it with documented reasoning.
   - **Risk if ignored**: The migration is vulnerable to reversal by a future agent who correctly identifies IX's "prefer" language and argues VI was held to a double standard.

6. **Characterize VI migration as scope-qualifier failure, not strength reduction** (Priority: P2)
   - **Current state**: The review task frames the MUST→SHOULD question as whether migration "smuggled a strength reduction." VI already uses SHOULD at line 353. The strength was not changed by migration.
   - **Proposed change**: The v3.0.0 SIR MUST state: "VI's migration rationale is scope-qualifier failure under Criterion 2. VI already used SHOULD; the migration introduces no strength reduction. The SHOULD was unenforceable at constitutional level not because of its strength but because the condition triggering it ('when the artifact drives behavior') could not be automatically checked. The receiving operational guidance document MAY carry the same SHOULD or strengthen to MUST where the scope is made falsifiable (e.g., for load-trigger-referenced files specifically)."
   - **Rationale**: If the SIR incorrectly describes the migration as a strength reduction, future reviewers will argue "no strength was lost, so the gate's purpose (enforcement) was not served by migration." This mischaracterization would undermine the migration's rationale.
   - **Risk if ignored**: The audit trail contains a factual error about VI's pre-migration normative strength, creating a reversal argument that is correct on the factual dimension even if wrong on the gate-criterion analysis.

7. **Cross-reference XVI's Criterion 1 path from the retained constitutional text** (Priority: P2)
   - **Current state**: XVI's Enforcement bullet at lines 742–746 names the CI lint path. The SIR follow-up TODO at line 181 confirms the path is concrete. However, the link between the constitutional text and the lint's TODO is visible only by reading both the principle body and the SIR comment block.
   - **Proposed change**: In the retained XVI constitutional text (post-path-c migration), add a parenthetical to the Enforcement bullet: "CI lint detecting re-entrant `GapFiller.fill()` calls (see spec 068 follow-up; script at `scripts/lint-gapfiller.py`)." This makes the Criterion 1 sketch visible from within the principle text itself without SIR archaeology.
   - **Rationale**: Gate Criterion 1 requires that "the path to building [the check] MUST be concrete enough that an engineer reading the principle can sketch the check in one paragraph." The check is concrete; the cross-reference makes it visible to an engineer reading only the principle. The SIR follow-up TODO is not guaranteed to be read by future principle reviewers.
   - **Risk if ignored**: A future reviewer auditing XVI's constitutional standing cannot confirm Criterion 1 compliance without reading the SIR comment chain, which is buried in HTML comment blocks and easily overlooked.

8. **Preserve XVI's plain-language clause as MUST in the receiving operational guidance** (Priority: P2)
   - **Current state**: The plain-language sub-bullet at lines 694–698 uses MUST: "Plugin recommendations... MUST include plain-language explanations." If this clause is migrated to operational guidance, the receiving document's normative strength for this requirement is unspecified.
   - **Proposed change**: The receiving operational guidance document MUST carry this clause at MUST strength, not advisory: "Plugin recommendations that output equilibrium scores, convergence predictions, or numerical config suggestions MUST include a plain-language explanation of each numerical output (example: '87% of agents are at their best possible position given others' positions')." The example form from the current text is mechanically helpful even if the adequacy of a given explanation is not machine-checkable.
   - **Rationale**: Migration should be a reclassification, not a strength reduction. The plain-language clause's Criterion 2 failure is that compliance cannot be automatically verified — it is not that the requirement is unimportant. Operational guidance is the correct home for "judgment calls" (per gate text line 1231); it can carry MUST requirements for judgment-laden behavior.
   - **Risk if ignored**: Plugin implementations ship with bare numerical outputs ("equilibrium quality: 0.87") and the user-transparency intent of XVI's original vision is silently abandoned.

9. **Acknowledge the override-precedent relevance to XVI path (c)** (Priority: P3)
   - **Current state**: The v2.5.0 SIR at lines 44–51 establishes an override-with-rationale precedent: when a blind reviewing standard would relegate a principle to operational guidance but the diff-shape consistency IS substantively verifying for the dominant failure mode, the override is justified with rationale logged. This precedent is directly relevant to XVI path (c): the blind v2 reviewing standard ("acknowledged residual = operational guidance") would relegate XVI entirely, but XVI's pinning discipline IS substantively verifying for the re-resolution failure mode.
   - **Proposed change**: The v3.0.0 SIR SHOULD explicitly invoke the override precedent established at lines 44–51 to justify XVI's partial retention rather than full migration: "Per the override-with-rationale precedent established in the v2.5.0 SIR (lines 44–51): XVI's pinning discipline is substantively verifying for the re-parameter-resolution failure pattern; the plain-language clause's acknowledged residual does not force full migration. Partial retention under path (c) is the correct application of the precedent."
   - **Rationale**: The override precedent exists; the SIR should use it where applicable rather than presenting path (c) as a novel exception. Using established precedent strengthens the audit trail's coherence.
   - **Risk if ignored**: Path (c) appears to be an ad hoc exception to the normal gate application, which weakens its authority. Invoking the established precedent gives it the same standing as the XXVIII override.

---

### Referenced Documentation

- `CONSTITUTION-v2.5.0-pre-migration.md` — cited sections:
  - Lines 102–111: Grandfathering disclosure (explicit failure labels for VI, X, XVI)
  - Lines 349–362: Principle VI (Scripts Over Markdown — full text including operative condition and carve-out)
  - Lines 403–482: Principle IX (Functional Programming — full text including behavior-over-shape extension and mechanical MUST block)
  - Lines 484–503: Principle X (Zen of Python Output — full text including summary/final.md sub-bullet)
  - Lines 622–653: Principle XV (Plugin Isolation — full text including PluginResult interface enforcement)
  - Lines 655–758: Principle XVI (Mathematical Transparency — full text including pinning discipline, plain-language sub-bullet, and v2.3.2 clarification block)
  - Lines 742–746: XVI Enforcement bullet (CI lint TODO for re-entrant GapFiller.fill())
  - Lines 967–996: Principle XXIV (Safety-Critical Defense-in-Depth — scope enumeration and three-layer defense)
  - Lines 1107–1149: Principle XXVIII (Test-Fix Boundary Preservation — override precedent source)
  - Lines 44–51: v2.5.0 SIR override-with-rationale block (override precedent)
  - Lines 178–181: v2.3.1→v2.3.2 SIR (CI lint follow-up TODO for GapFiller)
  - Lines 1196–1265: Governance section — Constitutional Inclusion Criteria (Criteria 1–3 full text, worked examples, prospective-only clause, operational guidance routing rule)