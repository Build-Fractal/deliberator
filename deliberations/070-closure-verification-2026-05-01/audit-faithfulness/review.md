I'll read the implementation files to get precise line numbers before writing the review.

Now I have all the data I need. Let me write the review.

### Executive Summary

This deliberation audits whether the implementation of spec 070's three prescribed remediations — migration of Principle VI to `CONTRIBUTING.md`, migration of Principle X to `docs/output-conventions.md`, and Option A headline rewrite of Principle XVI — faithfully matches the per-principle prescriptions in §4. The implementation spans two amendment cycles: v2.5.0→v2.6.0 (cycle 1: XVI rewrite) and v2.6.0→v3.0.0 (cycle 2: VI and X migration). Across both cycles, the migrations are substantively sound: tombstones are correctly formed, normative strength is appropriately downgraded, and the destination documents carry the essential guidance categories the spec prescribed. One substantive gap exists: spec 070 §4.1 explicitly prescribed concrete examples drawn from current `skills/`, `presets/`, and `templates/` directory usage as part of the VI migration content; `CONTRIBUTING.md` contains no such examples. Two organizational gaps exist in `docs/output-conventions.md`: the `summary/final.md` entry-point and directory-depth bullets — both explicitly identified by spec 070 §4.2(b) as mechanically-checkable — land in the aesthetic Conventions section rather than the Recommended implementation patterns section. A deferred Governance operational-guidance list update (acknowledged in the SIR at L384–386 as a P2 follow-up) remains outstanding. The XVI rewrite authorized one notable refinement: blind verification drove the headline to ONE structural invariant (parameter pinning) where spec 070 §4.3 Option A literally named three; this refinement is authorized by spec 067's protocol and constitutionally superior to the spec's literal wording. The single most important action is to add concrete `skills/`, `presets/`, and `templates/` examples to `CONTRIBUTING.md` §  Authoring Conventions — the spec said to include them, and they are absent.

### Alignment

- **VI tombstone — correct form** (`CONSTITUTION.md` L935–941): The tombstone for Principle VI carries all five Removal checklist elements required by the Governance § Removal checklist: strikethrough headline (`~~Scripts Over Markdown~~`), retirement date (2026-05-01), retirement version (v3.0.0), migration target (`CONTRIBUTING.md` § Authoring Conventions), and no-reuse declaration cross-referencing Principle Number Stability. The format precisely satisfies the spec 070 §5.3 success criterion requiring a tombstone entry.

- **X tombstone — correct form** (`CONSTITUTION.md` L1063–1069): Parallel to VI: all five Removal checklist elements are present. Migration target named as `docs/output-conventions.md`, no-reuse declaration present. Fully satisfies spec 070 §5.3 criterion (c).

- **XVI headline — ONE invariant** (`CONSTITUTION.md` L1221–1226): The current headline reads "conversus MUST satisfy one structural invariant: **parameter pinning** — resolved values are persisted once and re-loaded, never re-resolved within a deliberation run." This is exactly the structural load-bearing claim spec 070 §4.3 Option A called for when it prescribed that the headline elevate the falsifiable structural contract. The SIR (L476: "Headline consequence: one named invariant (parameter pinning)") correctly records the blind verification ruling that drove the single-invariant form.

- **XVI stage-3 attribution** (`CONSTITUTION.md` L1245–1251): Stage 3 reads: "**Deterministic assembly**: applies Principles VII and VIII to the optimization domain — … No additional normative requirement beyond Principles VII and VIII is introduced at this stage." This precisely satisfies spec 070 §4.3 Option A's requirement that the stage-3 body attribute its guarantee to Principles VII and VIII. The wording "no additional normative requirement" is the exact clause that keeps XVI from overreaching its constitutional mandate.

- **XVI design intent relocation** (`CONSTITUTION.md` L1352–1359): The Origin note closes with "Design intent: that users understand what is being optimized without understanding the math." This is the spec 070 §4.3-prescribed repositioning of the "user MUST understand" framing from the headline/body to design-intent status. The label "Design intent:" is explicit, unambiguous, and matches the SIR's attestation at L493–497.

- **Normative strength shift** (`CONTRIBUTING.md` L20–30; `docs/output-conventions.md` L12–27): Both destination documents use SHOULD consistently where the original principles used MUST or absolute phrasing. Spec 070 §4.1 prescribed "tone shifts from 'MUST' to 'prefer'" and §4.2 prescribed "aesthetic intent in operational-guidance language." The SHOULD framing in `CONTRIBUTING.md` and `docs/output-conventions.md` correctly implements this, and the explanation paragraphs in each document articulate why SHOULD is appropriate (judgment-call qualifier makes MUST unenforceable without a mechanical check that cannot be built).

- **Cross-reference cleanup** (`CONSTITUTION.md` body lines): All body-text references to VI and X fall into three appropriate categories: tombstone slots at L935 and L1063, the Governance retirement notice at L1858–1869 (which is the mandated retirement documentation), and the Removal checklist item (f) methodology example at L1972 (which cites "Principle VI" and "Principles VI and X" as audit-methodology illustrations, not as cross-references to active principles). No stranded references exist. The SIR explicitly established the "singular form, plural form, and adjacent-phrase forms" audit methodology (L309–312) and it was applied.

### Missed Opportunities

- **Missing examples from skills/, presets/, templates/** (`CONTRIBUTING.md` L15–42): Spec 070 §4.1(c) explicitly prescribes "(c) examples drawn from current `skills/`, `presets/`, and `templates/` usage." The current `CONTRIBUTING.md` Authoring Conventions section contains three directional bullets about orchestration logic, YAML configuration, and human-orientation markdown, but no concrete examples from any of these three directories. A reader unfamiliar with the repo cannot tell from the current text what the difference looks like in practice. The prescription called for concrete examples precisely because "drives behavior" requires judgment, and examples are how operational guidance reduces that judgment to pattern-matching. Impact: **high** — without examples, the guidance is abstract where spec 070 explicitly asked for it to be concrete.

- **Checkable bullets in wrong section of output-conventions.md** (`docs/output-conventions.md` L10–27 vs. L29–46): Spec 070 §4.2(b) prescribes extracting "the mechanically-checkable bullets (directory depth, `summary/final.md` entry point, 'errors don't pass silently') into a separate 'checkable conventions' subsection that *could* be backed by lints." In the actual document, `summary/final.md` entry point (L17–18) and directory depth (L20–21) appear in the aesthetic **Conventions** section as SHOULDs alongside subjective guidance ("Flat is better than nested"), while only warnings for malformed output (L34–41) appears in the **Recommended implementation patterns** section. The spec's two-section model — aesthetic vs. mechanically-checkable — is present in structure but not cleanly implemented. A future engineer looking to add lints has no clear signal that `summary/final.md` and directory depth are the checkable targets. Impact: **medium** — all content is present; the misclassification weakens discoverability for linting work.

- **"Errors don't pass silently" not preserved** (`docs/output-conventions.md` L34–37): Spec 070 §4.2 frames the checkable subset as including "'errors don't pass silently'", the Zen of Python maxim. The document translates this as "Silent failure is worse than warned-and-continued behavior" — a semantic equivalent but not the recognizable phrasing. The original Zen phrasing has cultural signal value for Python engineers and was cited by spec 070 specifically because it is a known, quoted idiom. Losing the exact phrasing weakens the cultural anchor that made Principle X's aesthetic stance meaningful in the first place. Impact: **low** — the substance is preserved, only the phrasing is weakened.

- **Governance operational-guidance list not yet updated**: The v3.0.0 SIR (L384–386) explicitly deferred this: "The Governance section's operational-guidance list will be updated in a subsequent PATCH (P2 in the self verdict)." The current `CONSTITUTION.md` Governance section has no explicit list pointing readers to `CONTRIBUTING.md` and `docs/output-conventions.md` as the canonical operational-guidance destinations for VI and X content. Spec 070 §7 risk register identifies exactly this failure mode: "Future authors don't consult `CONTRIBUTING.md` / `docs/*.md` and re-introduce the migrated patterns." The cross-reference from Governance body to operational guidance is the primary mitigation. It is outstanding. Impact: **medium** — the deferred update is the document-level enforcement that makes the migration durable.

- **Heuristic phrasing location not self-advertising in bullets** (`CONTRIBUTING.md` L19–21): The heuristic "if a document is consumed by automation or agents to make decisions, it SHOULD be a script, config, or structured data" (L19–21) is present and semantically equivalent to spec 070 §4.1(b)'s prescribed phrasing. However, it sits in the opening prose paragraph, not as a dedicated bullet. The three bullets that follow are structural cases (orchestration logic, YAML config, human-orientation markdown) but do not include a standalone heuristic bullet. A reader skimming bullets will find the structural cases but may miss the decision rule that ties them together. Impact: **low** — the heuristic is present in the section, just not in the most scannable position.

- **XVI: spec 070 §4.3 literal prescription included three headline claims** (spec 070 §4.3 Option A): Spec 070 §4.3 Option A literally reads: "Rewrite Principle XVI so its headline claim is the falsifiable structural contract (parameter pinning, shape determinism, plain-language output pairing)." The implementation headline names only ONE claim (parameter pinning). This divergence is explicitly authorized: the v2.6.0 SIR (L476) records the blind verification ruling that drove single-invariant form, and spec 067's "take the more rigorous standard" protocol governs. The resulting headline is constitutionally superior to the spec's literal prescription. This is an authorized divergence, not a defect — but it is a divergence, and an audit that did not surface it would be incomplete. A future reader should find the authorization in the SIR, not discover the gap independently. Impact: **low** — the SIR is thorough; the main risk is that spec 070's §4.3 wording creates a textual discrepancy that could confuse future amendment authors citing the spec.

### Off-Base Assumptions

- **Assumption: spec 070 §4.1(c) examples were an optional detail, not a required element**: The implementation treats examples from `skills/`, `presets/`, and `templates/` as implied by the directional bullets rather than as a separate content requirement. Spec 070 §4.1 presents (a), (b), and (c) as a coordinate list with "the migrated content should be a one-page authoring-conventions note covering" as the umbrella. "(c) examples drawn from current `skills/`, `presets/`, and `templates/` usage" is structurally coequal with (a) and (b). Treating it as optional misreads the prescription's structure.

- **Assumption: the "checkable conventions" subsection is a naming convention, not a classification requirement**: The implementation creates a section called "Recommended implementation patterns" (output-conventions.md L29) which carries the intended checkable content. However, it places `summary/final.md` entry point and directory depth in the aesthetic Conventions section, apparently treating them as aesthetic guidance rather than mechanically-checkable assertions. Spec 070 §4.2(b) explicitly identified these as mechanically-checkable — the prescription was not about naming the section but about routing the right bullets into it. The misclassification reflects an assumption that the section boundary is cosmetic, where spec 070 intended it to be functional (pointing future lint authors to the checkable targets).

- **No off-base assumptions about Principle XVI's path (c) mechanics**: The implementation correctly applied the path (c) framework as defined by spec 070 §4.3 Option A and subsequently ratified in the Governance section at L1889–1905. The SIR's Criterion 3 formal record, the design-intent positioning, and the stage-3 attribution all match the spec's prescribed analytical structure. No misunderstanding of the XVI prescription is evident.

### Actionable Recommendations

1. **Add concrete directory examples to CONTRIBUTING.md** (Priority: P1)
   - **Current state**: `CONTRIBUTING.md` §  Authoring Conventions contains three directional bullets (L24–30) and a heuristic opening sentence (L19–21), but no examples from `skills/`, `presets/`, or `templates/` directories. Spec 070 §4.1(c) explicitly prescribes such examples.
   - **Proposed change**: Add a fourth subsection or an "Examples" block under the Scripts Over Markdown section. Include at least one positive example (e.g., a `presets/` YAML file as the correct pattern for agent configuration) and one anti-pattern to avoid (e.g., a hypothetical `orchestration-notes.md` that names phases an agent should execute, which should instead live in `SKILL.md` or a structured config). These should be real paths or realistic synthetic paths from the repo.
   - **Rationale**: Spec 070 §4.1's justification for migration was that "drives behavior" requires judgment; examples are the operational guidance mechanism that reduces judgment to pattern-matching. Without them, the CONTRIBUTING.md section is abstract where the spec explicitly required it to be concrete.
   - **Risk if ignored**: Reviewers applying the guidance will face the same interpretation problem the original principle faced — "does this file drive behavior?" — without the examples that help calibrate the answer. The migration succeeds structurally but fails to deliver the practical improvement that justified moving the content out of the constitution.

2. **Relocate summary/final.md and directory-depth bullets to the Recommended patterns section** (Priority: P2)
   - **Current state**: `docs/output-conventions.md` L17–21 places the `summary/final.md` entry-point bullet and the "one level deep" directory-depth bullet in the aesthetic Conventions section (L10–27). Spec 070 §4.2(b) classified both as mechanically-checkable and prescribed a separate subsection for them.
   - **Proposed change**: Move the `summary/final.md` (L16–18) and directory-depth (L19–21) bullets from the Conventions section to the Recommended implementation patterns section (after L29), with *Recommended check* annotations parallel to the existing warnings entry (L38–41). Adjust the Conventions section intro sentence to make clear it carries only the genuinely aesthetic guidance.
   - **Rationale**: The two-section model in spec 070 §4.2 is functional, not cosmetic — it signals to future engineers which bullets are lint candidates. Misclassifying checkable assertions as aesthetic guidance obscures the linting path.
   - **Risk if ignored**: Engineers implementing the CI lints anticipated in spec 070 §5.2 risk table entry for X will not find clear guidance on which output-convention bullets are lint targets. The deferred "split the mechanically-checkable subset into a new gate-passing principle" path (spec 070 §8 Q4) also depends on having those bullets clearly identified as checkable.

3. **Complete the deferred Governance operational-guidance list update** (Priority: P2)
   - **Current state**: The v3.0.0 SIR (L384–386) deferred this: "The Governance section's operational-guidance list will be updated in a subsequent PATCH." No such list exists in the current Governance body; the section does not point readers to `CONTRIBUTING.md` or `docs/output-conventions.md`.
   - **Proposed change**: Add a brief "Operational guidance" bullet to the Governance section listing `CONTRIBUTING.md` and `docs/output-conventions.md` as the canonical destinations for judgment-call authoring guidance that falls outside constitutional invariants. This is the cross-reference from constitution → operational guidance that spec 070 §7 risk register identifies as the primary mitigation for "future authors don't consult the new home."
   - **Rationale**: Spec 070 §5.3 success criterion 4 requires "Cross-references from other principles, specs, and skills point at the new location." The tombstones satisfy this for the principle slots. The Governance body also generates traffic — readers consulting the Constitutional Inclusion Criteria gate text for routing decisions need to find the operational-guidance homes without following tombstone links.
   - **Risk if ignored**: The migration's long-term durability depends on the constitution pointing at its operational-guidance documents. The SIR acknowledged this; it is still outstanding.

4. **Document the single-invariant XVI divergence in spec 070 §4.3 explicitly** (Priority: P2)
   - **Current state**: Spec 070 §4.3 Option A prescribes "headline claim is the falsifiable structural contract (parameter pinning, shape determinism, plain-language output pairing)" — three claims. The implementation reduced this to one (parameter pinning). The v2.6.0 SIR at L476 records the blind verification ruling, but spec 070 §4.3's wording is not updated to reflect the authorized refinement.
   - **Proposed change**: Annotate spec 070 §4.3's Option A description with a note that the blind verification ruling drove the headline to ONE invariant rather than three, citing the v2.6.0 SIR as the authorization. Alternatively, add a footnote or amendment note to the spec file.
   - **Rationale**: Spec 070 sits in `specs/done/` and will be cited by future migration authors. If they read Option A literally, they may try to name three structural claims in a headline for a different principle, citing spec 070 as precedent. The SIR record exists but requires following a path from the spec to the SIR. A note in the spec itself closes the loop cleanly.
   - **Risk if ignored**: Low in the short term, but future migration specs citing spec 070's XVI Option A wording as a precedent will get the wrong headline model.

5. **File a tracking issue for the Governance Criterion 1 worked-example correction** (Priority: P2)
   - **Current state**: The v3.0.0 SIR (CONSTITUTION.md L293–296) flags that "The Governance section's existing Criterion 1 worked example ('code should be readable') exhibits the same Criterion 1/2 conflation pattern; that worked example is flagged here for future correction." No tracking issue for this correction appears in the provided files.
   - **Proposed change**: File a GitHub issue to correct the Criterion 1 worked example in the Governance § Constitutional Inclusion Criteria section. The current worked example ("code should be readable") fails Criterion 1 on its own terms (no automated check is feasible) and was already identified by the SIR as a Criterion 2 failure (falsifiable scope). The correction should replace the example with one that unambiguously exercises Criterion 1.
   - **Rationale**: The SIR's own prose on the Criterion 1/2 distinction (L289–300) is substantively important for future amendment authors. A worked example that conflates the two undermines that prose. The SIR flagged this; it should not become a permanent dead-letter in the SIR record.
   - **Risk if ignored**: Amendment authors applying the gate will inherit the conflation, leading to mislabeled migration rationales in future SIRs (the same error the v3.0.0 SIR corrected for VI).

6. **Restore "errors don't pass silently" Zen phrasing** (Priority: P3)
   - **Current state**: `docs/output-conventions.md` L36–37 reads "Silent failure is worse than warned-and-continued behavior" — a semantic paraphrase of the Zen of Python maxim "errors should never pass silently."
   - **Proposed change**: Replace the paraphrase with the Zen phrasing, or add it parenthetically: "Silent failure is worse than warned-and-continued behavior (Zen of Python: 'errors should never pass silently')."
   - **Rationale**: Spec 070 §4.2 chose this example precisely because it is a named, culturally loaded idiom that carries the aesthetic stance succinctly. The document's raison d'être is to preserve the Zen of Python framing in operational-guidance form; losing the phrasing weakens that framing.
   - **Risk if ignored**: Minor. The substance is preserved; only the cultural anchor is weakened.

7. **Add a "Checkable conventions" label to the Recommended patterns subsection** (Priority: P3)
   - **Current state**: `docs/output-conventions.md` L29 heads the checkable section "Recommended implementation patterns." Spec 070 §4.2(b) prescribed "a separate 'checkable conventions' subsection that *could* be backed by lints."
   - **Proposed change**: Rename or relabel the section from "Recommended implementation patterns" to "Checkable conventions" (or "Checkable conventions and recommended implementation patterns") to explicitly signal to engineers that this subsection is the lint-candidate pool.
   - **Rationale**: The subsection's purpose is discovery for linting work; the name should signal that purpose directly. "Recommended implementation patterns" is accurate but not as actionable as "Checkable conventions."
   - **Risk if ignored**: Minor. The label is discoverable through context. This is polish, not substance.

8. **Verify cross-references in skill files point to new locations** (Priority: P2)
   - **Current state**: Spec 070 §5.3 success criterion 4 requires "Cross-references from other principles, specs, and skills point at the new location." The review confirmed tombstones in `CONSTITUTION.md` and cross-reference in `CONTRIBUTING.md` point correctly. Skill files (`skills/`, `presets/`, agent configurations) have not been audited in this review.
   - **Proposed change**: Run a cross-reference audit on `skills/**` and `presets/**` files for any references to "Principle VI," "Principle X," "Scripts Over Markdown," or "Zen of Python Output" that should be updated to point at `CONTRIBUTING.md` § Authoring Conventions or `docs/output-conventions.md`.
   - **Rationale**: The SIR establishes a "singular form, plural form, and adjacent-phrase forms" audit methodology (L309–312) for body-text cross-references. That methodology was applied to `CONSTITUTION.md`. The same methodology should apply to operational files that might reference the retired principles.
   - **Risk if ignored**: Medium if any skill files reference the retired principles by name — those references become dead or misleading as agents consult them.

### Referenced Documentation

- `specs/done/070-grandfathered-audit/spec.md` — §4.1 (VI migration prescription), §4.2 (X migration prescription), §4.3 (XVI Option A prescription), §5.3 (success criteria), §6 (verification requirements), §7 (risk register), §8 Q4 (X checkable subset follow-up)
- `CONSTITUTION.md` — L935–941 (VI tombstone), L1063–1069 (X tombstone), L1221–1226 (XVI headline), L1245–1251 (stage-3 attribution), L1352–1359 (XVI design intent Origin note), L1858–1869 (Governance grandfathering retirement paragraph), L1889–1905 (path (c) definition), L1972 (Removal checklist item (f) methodology), L384–386 (SIR: deferred Governance operational-guidance list update), L293–296 (SIR: Criterion 1 worked-example correction flag), L476 (SIR: single-invariant headline consequence)
- `CONTRIBUTING.md` — L15–42 (Authoring Conventions section, Scripts Over Markdown, Output Conventions cross-reference)
- `docs/output-conventions.md` — L10–27 (Conventions section), L17–18 (summary/final.md entry point), L20–21 (directory depth), L29–46 (Recommended implementation patterns), L34–41 (Warnings for malformed output), L48–64 (Why operational guidance section)