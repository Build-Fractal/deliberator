# Feature Extraction

Deterministic text-to-vector extraction from deliberation artifacts, producing fixed-width feature vectors for downstream analysis (spec 015).

::: deliberator.schemas.extraction
    options:
      members:
        - extract_features
        - write_features
